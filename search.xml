<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>SpringWebMvc</title>
    <url>/2021/09/07/spring/springWebMvc/</url>
    <content><![CDATA[<p>SpringWebMVC是构建在ServletAPI上的最初的Web框架，从一开始就包含在Spring框架中。正式名称“SpringWebMVC”来自其源模块(Spring-webmvc)的名称，但它更常见的名称是“SpringMVC”。与SpringWebMVC并行，SpringFramework5.0引入了一个反应性堆栈Web框架，它的名称“SpringWebFlux”也是基于它的源模块(SpringWebFlux)。</p>
<p>与许多其他Web框架一样，SpringMVC是围绕前端控制器模式设计的，其中一个中心servlet DispatcherServlet为请求处理提供了一个共享算法，而实际工作则由可配置的委托组件执行。该模型具有灵活性，支持多种工作流。与任何servlet一样，DispatcherServlet需要使用Java配置或web.xml根据servlet规范进行声明和映射。反过来，DispatcherServlet使用Spring配置来发现请求映射、视图解析、异常处理等所需的委托组件。</p>
<p>SpringBoot遵循不同的初始化顺序。SpringBoot没有连接到servlet容器的生命周期，而是使用Spring配置引导自身和嵌入式servlet容器。<code>Filter</code>和<code>Servlet</code>声明在Spring配置中,容器检测到并在servlet容器中注册. </p>
<p>传统的Spring和Springmvc依赖于servlet容器的生命周期，通过org.springframework.web.context.ContextLoaderListener 容器监听器和org.springframework.web.servlet.DispatcherServlet 注册 来完成容器的启动 这与SpringBoot 通过容器初始化，通过工厂方式创建WebServer ，然后监听ServletContext ,并回调</p>
<h1 id="上下文层次结构"><a href="#上下文层次结构" class="headerlink" title="上下文层次结构"></a>上下文层次结构</h1><p>DispatcherServlet 可以直接与 ServletContext 进项关联 xml，或者借助 WebApplicationContext 与 ServletContext 进项关联(JavaConfig) ，如果应用程序想要获取WebApplicationContext，可通过RequestContextUtils 工具类获取到它.通常情况下WebApplicationContext 有一个上下文层次结构，应用程序环境有父子容器关系，根<code>ApplicationContext</code>通常包含基础设施bean（扫描service包）,这些bean实际上是继承的，可以在WebApplicationContext （扫描controller包）特定的子级中重写(即重新声明),下图显示了这种关系，</p>
<p><img src="/images/mvc-context-hierarchy.png"></p>
<h1 id="DispatcherServlet"><a href="#DispatcherServlet" class="headerlink" title="DispatcherServlet"></a>DispatcherServlet</h1><p>DispatcherServlet 就像Servlet 链接到ServletContext 有多重方式，支持web.xml,或者JAVA 配置方式 来进项链接.（Tomcat容器方式）</p>
<h2 id="web-xml-方式类链接-DispatcherServlet"><a href="#web-xml-方式类链接-DispatcherServlet" class="headerlink" title="web.xml 方式类链接 DispatcherServlet"></a>web.xml 方式类链接 DispatcherServlet</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;web-app&gt;</span><br><span class="line">     &lt;!--ROOTApplicationContext--&gt;</span><br><span class="line">    &lt;listener&gt;</span><br><span class="line">        &lt;listener-class&gt;org.springframework.web.context.ContextLoaderListener&lt;/listener-class&gt;</span><br><span class="line">    &lt;/listener&gt;</span><br><span class="line">    &lt;context-param&gt;</span><br><span class="line">        &lt;param-name&gt;contextConfigLocation&lt;/param-name&gt;</span><br><span class="line">        &lt;param-value&gt;classpath:spring.xml,classpath:dubbo-consumer.xml&lt;/param-value&gt;</span><br><span class="line">    &lt;/context-param&gt;</span><br><span class="line">      &lt;!--注册DispatcherServlet--&gt;</span><br><span class="line">    &lt;servlet&gt;</span><br><span class="line">        &lt;servlet-name&gt;dispatcher&lt;/servlet-name&gt;</span><br><span class="line">        &lt;servlet-class&gt;org.springframework.web.servlet.DispatcherServlet&lt;/servlet-class&gt;</span><br><span class="line">        &lt;init-param&gt;</span><br><span class="line">            &lt;param-name&gt;contextConfigLocation&lt;/param-name&gt;</span><br><span class="line">            &lt;param-value&gt;classpath:spring-mvc.xml&lt;/param-value&gt;</span><br><span class="line">        &lt;/init-param&gt;</span><br><span class="line">        &lt;load-on-startup&gt;1&lt;/load-on-startup&gt;</span><br><span class="line">    &lt;/servlet&gt;</span><br><span class="line">    &lt;servlet-mapping&gt;</span><br><span class="line">        &lt;servlet-name&gt;dispatcher&lt;/servlet-name&gt;</span><br><span class="line">        &lt;url-pattern&gt;/*&lt;/url-pattern&gt;</span><br><span class="line">    &lt;/servlet-mapping&gt;</span><br><span class="line"></span><br><span class="line">&lt;/web-app&gt;</span><br></pre></td></tr></table></figure>

<h2 id="WebApplicationInitializer-方式类链接-DispatcherServlet"><a href="#WebApplicationInitializer-方式类链接-DispatcherServlet" class="headerlink" title="WebApplicationInitializer 方式类链接 DispatcherServlet"></a>WebApplicationInitializer 方式类链接 DispatcherServlet</h2><p>现在JavaConfig配置方式在逐步取代xml配置方式。而WebApplicationInitializer可以看做是Web.xml的替代，它是一个接口。通过实现WebApplicationInitializer，在其中可以添加servlet，listener等，在加载Web项目的时候会加载这个接口实现类，从而起到web.xml相同的作用</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class MyWebApplicationInitializer implements WebApplicationInitializer &#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void onStartup(ServletContext servletContext) &#123;</span><br><span class="line">    </span><br><span class="line">        // Load Spring web application configuration</span><br><span class="line">        AnnotationConfigWebApplicationContext context = new AnnotationConfigWebApplicationContext();</span><br><span class="line">        //可采用注解的方式替代xml配置</span><br><span class="line">        context.register(AppConfig.class);</span><br><span class="line">        </span><br><span class="line">        // Create and register the DispatcherServlet</span><br><span class="line">        DispatcherServlet servlet = new DispatcherServlet(context);</span><br><span class="line">        ServletRegistration.Dynamic registration = servletContext.addServlet(&quot;app&quot;, servlet);</span><br><span class="line">        registration.setLoadOnStartup(1);</span><br><span class="line">        registration.addMapping(&quot;/*&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>或者 采用xml</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class MyWebApplicationInitializer implements WebApplicationInitializer &#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void onStartup(ServletContext container) &#123;</span><br><span class="line">        XmlWebApplicationContext appContext = new XmlWebApplicationContext();</span><br><span class="line">        appContext.setConfigLocation(&quot;/WEB-INF/spring-mvc.xml&quot;);</span><br><span class="line"></span><br><span class="line">        ServletRegistration.Dynamic registration = container.addServlet(&quot;dispatcher&quot;, new DispatcherServlet(appContext));</span><br><span class="line">        registration.setLoadOnStartup(1);</span><br><span class="line">        registration.addMapping(&quot;/*&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h1 id="特殊的Bean"><a href="#特殊的Bean" class="headerlink" title="特殊的Bean"></a>特殊的Bean</h1><p>DispatcherServlet接受请求并强请求委派给SpringMvc中定义的相应功能Bean进项处理，这些Bean担任不同的角色，有者不同的处理能力，这些通常带有内置的契约，我们可以自定义他们的属性或者替换他们来完成我们的业务需求</p>
<table>
<thead>
<tr>
<th align="left">Bean</th>
<th align="left">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="left">HandlerMapping</td>
<td align="left">将请求映射到处理程序以及生成拦截器处理链。映射是基于一些标准，其细节取决于HandlerMapping实现。两主HandlerMapping实现是RequestMappingHandlerMapping(支持@RequestMapping)和SimpleUrlHandlerMapping(它维护对处理程序的URI路径模式的显式注册)。</td>
</tr>
<tr>
<td align="left">HandlerAdapter</td>
<td align="left">调用映射到请求的处理程序，可适配多种具体的执行方式，如处理程序为handlerMethod Controller HandlerFunction  Servlet HttpRequestHandler，HandlerAdapter主要目的是隐藏调用细节，简化DispatcherServlet，让DispatcherServlet专注于流程控制</td>
</tr>
<tr>
<td align="left">HandlerExceptionResolver</td>
<td align="left">mvc中解决异常的策略，可以将程序中的执行异常，映射到处理程序，HTML试图，或者其他结果，常见的HandlerExceptionResolver实现有，SimpleMappingExceptionResolver，DefaultHandlerExceptionResolver，ResponseStatusExceptionResolver，ExceptionHandlerExceptionResolver</td>
</tr>
<tr>
<td align="left">ViewResolver</td>
<td align="left">如果处理程序返回视图数据模型，那么ViewResolver负责对视图数据模型进行解析,可根据具体的请求接受类型，返回最适合的视图View，并由View进行视图的渲染</td>
</tr>
<tr>
<td align="left">LocaleResolver,LocaleContextResolver</td>
<td align="left">解析Locale客户端正在使用并可能使用他们的时区，以便能够提供国际化的视图</td>
</tr>
<tr>
<td align="left">ThemeResolver</td>
<td align="left">解决您的Web应用程序可以使用的主题</td>
</tr>
<tr>
<td align="left">MultipartResolver</td>
<td align="left">抽象用于解析多部分请求(例如，浏览器表单文件上传)</td>
</tr>
<tr>
<td align="left">FlashMapManager</td>
<td align="left">存储和检索“输入”和“输出”FlashMap它可以用于将属性从一个请求传递到另一个请求</td>
</tr>
</tbody></table>
<h1 id="doDispatch执行流程"><a href="#doDispatch执行流程" class="headerlink" title="doDispatch执行流程:"></a>doDispatch执行流程:</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">protected void doDispatch(HttpServletRequest request, HttpServletResponse response) throws Exception &#123;</span><br><span class="line">         .....</span><br><span class="line">		try &#123;</span><br><span class="line">			ModelAndView mv = null;</span><br><span class="line">			Exception dispatchException = null;</span><br><span class="line">			try &#123;</span><br><span class="line">			    //-------MultipartResolver 解析并装饰 request</span><br><span class="line">				processedRequest = checkMultipart(request);</span><br><span class="line">				//-------遍历HandlerMapping 并找到匹配的 处理链 HandlerExecutionChain</span><br><span class="line">				mappedHandler = getHandler(processedRequest);</span><br><span class="line">				if (mappedHandler == null) &#123;noHandlerFound(processedRequest, response);return;&#125;</span><br><span class="line">				//-------遍历HandlerAdapter 找到支持执行程序的适配器</span><br><span class="line">				HandlerAdapter ha = getHandlerAdapter(mappedHandler.getHandler());</span><br><span class="line">				//-------applyPreHandle 拦截器前置处理执行</span><br><span class="line">				if (!mappedHandler.applyPreHandle(processedRequest, response)) &#123;return;&#125;</span><br><span class="line">				//-------执行器执行</span><br><span class="line">				mv = ha.handle(processedRequest, response, mappedHandler.getHandler());</span><br><span class="line">				 //-------applyPostHandle 拦截器后置处理执行</span><br><span class="line">				mappedHandler.applyPostHandle(processedRequest, response, mv);</span><br><span class="line">			&#125;</span><br><span class="line">			catch (Exception ex) &#123;dispatchException = ...;&#125;</span><br><span class="line">			catch (Throwable err) &#123;dispatchException =...;&#125;</span><br><span class="line">			 //-------异常解析，视图解析 ，视图渲染  拦截器afterCompletion处理执行</span><br><span class="line">			processDispatchResult(processedRequest, response, mappedHandler, mv, dispatchException);</span><br><span class="line">		&#125;</span><br><span class="line">		catch (Exception ex) &#123;</span><br><span class="line">		     //-------拦截器afterCompletion处理执行</span><br><span class="line">			triggerAfterCompletion</span><br><span class="line">		&#125;</span><br><span class="line">		catch (Throwable err) &#123;</span><br><span class="line">		   //-------拦截器afterCompletion处理执行</span><br><span class="line">			triggerAfterCompletion</span><br><span class="line">		&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="问题："><a href="#问题：" class="headerlink" title="问题："></a>问题：</h1><ol>
<li>HandlerMapping实现都有哪些，何时被初始化  如何使用扩展</li>
<li>HandlerAdapter实现都有哪些，何时被初始化 如何使用扩展</li>
<li>HandlerExceptionResolver 实现都有哪些，何时被初始化  如何使用扩展            </li>
<li>ViewResolver   实现都有哪些，何时被初始化   如何使用扩展      </li>
<li>LocaleResolver,LocaleContextResolver        实现都有哪些，何时被初始化 如何使用扩展            </li>
<li>ThemeResolver 实现都有哪些，何时被初始化 如何使用扩展          </li>
<li>MultipartResolver   实现都有哪些，何时被初始化 如何使用扩展          </li>
<li>FlashMapManager 实现都有哪些，何时被初始化 如何使用扩展          </li>
</ol>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>springmvc</tag>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringWebMvc-HandlerMapping</title>
    <url>/2021/09/08/spring/HandlerMapping/</url>
    <content><![CDATA[<p>   HandlerMapping 将请求映射到具体的处理程序（如HandlerMethod Controller HandlerFunction Servlet HttpRequestHandler等），并缓存映射关系，当请求进入DispatcherServlet后，DispatcherServlet 遍历 注册 HandlerMapping 根据请求情况获取HandlerExecutionChain，HandlerExecutionChain中包含了具体的处理程序和拦截器列表<strong>，后续的执行中就直接使用了HandlerExecutionChain来处理，</strong>此时HandlerMapping  就完成了任务，就是构建HandlerExecutionChain。</p>
<p>   请求是通过DispatcherServlet 来进行统一处理的，那么DispatcherServlet  是如何探测到HandlerMapping的呢，我们如何才能注册自己的HandlerMapping呢</p>
<p>   HandlerMapping 只定义了根据请求获取处理链的接口方法，那么HandlerMapping 主要目的就是构建处理链，提供具体的处理器和拦截器链，那么拦截器是如何注册，处理器是如何注册的，并且是如何通过url匹配到拦截器和处理器，并封装成HandlerExecutionChain 就是HandlerMapping 要做的事</p>
<p>   SprignMvc中能够处理请求的处理器有很多，那么都有哪些可用的线程的处理器呢，如果不使用现有的处理器类型，如何实现自己的处理器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">HandlerMapping</span> </span>&#123;</span><br><span class="line">	<span class="function">HandlerExecutionChain <span class="title">getHandler</span><span class="params">(HttpServletRequest request)</span> <span class="keyword">throws</span> Exception</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="HandlerMapping探测"><a href="#HandlerMapping探测" class="headerlink" title="HandlerMapping探测"></a>HandlerMapping探测</h1><p>DispatcherServlet 继承自FrameworkServlet ，并且实现了onRefresh 监听WebApplicationContext刷新方法，完成MVC各个处理节点处理策略的初始化，其中HandlerMapping就是其中之一，</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onRefresh</span><span class="params">(ApplicationContext context)</span> </span>&#123;</span><br><span class="line">		initStrategies(context);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">initStrategies</span><span class="params">(ApplicationContext context)</span> </span>&#123;</span><br><span class="line">    initMultipartResolver(context);</span><br><span class="line">    initLocaleResolver(context);</span><br><span class="line">    initThemeResolver(context);</span><br><span class="line">    initHandlerMappings(context);</span><br><span class="line">    initHandlerAdapters(context);</span><br><span class="line">    initHandlerExceptionResolvers(context);</span><br><span class="line">    initRequestToViewNameTranslator(context);</span><br><span class="line">    initViewResolvers(context);</span><br><span class="line">    initFlashMapManager(context);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>具体探测步骤是</p>
<ol>
<li>根据detectAllHandlerMappings（是否探测所有的HandlerMapping）属性 true 完成 从bean容器中获取所有的HandlerMapping</li>
<li>根据detectAllHandlerMappings（是否探测所有的HandlerMapping）属性 false 完成 从bean容器中获取beanName 为handlerMapping 类型为HandlerMapping</li>
<li>如果都没有，就从默认DispatcherServlet.properties 配置中获取，spring-webmvc 包下配置了默认的策略规则HandlerMapping的默认策略规则为</li>
</ol>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">org.springframework.web.servlet.HandlerMapping</span>=<span class="string">org.springframework.web.servlet.handler.BeanNameUrlHandlerMapping,\</span></span><br><span class="line"><span class="string">   org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping,\</span></span><br><span class="line"><span class="string">   org.springframework.web.servlet.function.support.RouterFunctionMapping</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">initHandlerMappings</span><span class="params">(ApplicationContext context)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>.handlerMappings = <span class="keyword">null</span>;</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.detectAllHandlerMappings)</span><br><span class="line"><span class="comment">//探测所有的HandlerMapping</span></span><br><span class="line">			Map&lt;String, HandlerMapping&gt; matchingBeans =</span><br><span class="line">					BeanFactoryUtils.beansOfTypeIncludingAncestors(context, HandlerMapping.class, <span class="keyword">true</span>, <span class="keyword">false</span>);</span><br><span class="line">			<span class="keyword">if</span> (!matchingBeans.isEmpty()) &#123;</span><br><span class="line">				<span class="keyword">this</span>.handlerMappings = <span class="keyword">new</span> ArrayList&lt;&gt;(matchingBeans.values());</span><br><span class="line"><span class="comment">//执行order排序</span></span><br><span class="line">				AnnotationAwareOrderComparator.sort(<span class="keyword">this</span>.handlerMappings);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">				HandlerMapping hm = context.getBean(HANDLER_MAPPING_BEAN_NAME, HandlerMapping.class);</span><br><span class="line">				<span class="keyword">this</span>.handlerMappings = Collections.singletonList(hm);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.handlerMappings == <span class="keyword">null</span>) &#123;</span><br><span class="line"><span class="comment">//默认策略配置</span></span><br><span class="line">			<span class="keyword">this</span>.handlerMappings = getDefaultStrategies(context, HandlerMapping.class);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>



<h1 id="HandlerInterceptor-探测"><a href="#HandlerInterceptor-探测" class="headerlink" title="HandlerInterceptor 探测"></a>HandlerInterceptor 探测</h1><img src="/2021/09/08/spring/HandlerMapping/HandlerMappingClassH.png" class="" title="HandlerMapping结构图">

<h2 id="HandlerMapping顶级实现类AbstractHandlerMapping"><a href="#HandlerMapping顶级实现类AbstractHandlerMapping" class="headerlink" title="HandlerMapping顶级实现类AbstractHandlerMapping"></a>HandlerMapping顶级实现类AbstractHandlerMapping</h2><p>AbstractHandlerMapping是HandlerMapping的顶级实现类，所有的子类都直接或者间接的继承自这个类，AbstractHandlerMapping因为继承自WebApplicationObjectSupport抽象类，那么他就有了ServletContext （ServletContextAware setServletContext 接口方法）感知能力和ApplicationContext感知能力（initApplicationContext方法），<strong>那么HandlerInterceptor 就是在AbstractHandlerMapping的initApplicationContext中直接从bean容器中检测HandlerInterceptor  类型的</strong> 获取的类型为（**<u>MappedInterceptor</u>**）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">initApplicationContext</span><span class="params">()</span> <span class="keyword">throws</span> BeansException </span>&#123;</span><br><span class="line"></span><br><span class="line"><span class="comment">//SkipPathExtensionContentNegotiation</span></span><br><span class="line">	extendInterceptors(<span class="keyword">this</span>.interceptors);</span><br><span class="line">	</span><br><span class="line"><span class="comment">//检测容器中的HandlerMapping 并缓存</span></span><br><span class="line">	detectMappedInterceptors(<span class="keyword">this</span>.adaptedInterceptors);</span><br><span class="line">	</span><br><span class="line"><span class="comment">//合并extendInterceptors和detectMappedInterceptors 到adaptedInterceptors 中进行缓存</span></span><br><span class="line">	initInterceptors();</span><br><span class="line">	</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//检测容器中的HandlerMapping</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">detectMappedInterceptors</span><span class="params">(List&lt;HandlerInterceptor&gt; mappedInterceptors)</span> </span>&#123;</span><br><span class="line">	mappedInterceptors.addAll(BeanFactoryUtils.beansOfTypeIncludingAncestors(obtainApplicationContext(), 																					MappedInterceptor.class,<span class="keyword">true</span>, <span class="keyword">false</span>).values());</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p><strong>MappedInterceptor</strong> 内部缓存了匹配资源和排除匹配资源，并且支持提供匹配函数来对请求的资源进行匹配判断，支持针对不同的资源进行不同的拦截，通常情况下我们使用的就是这种匹配型拦截器，定义HandlerInterceptor   并把它放入MappedInterceptor，并配置MappedInterceptor的匹配属性，进行使用。</p>
<p>匹配型拦截器：includePatterns ，excludePatterns interceptor，</p>
<p>非匹配性拦截器： interceptor</p>
<p>构建HandlerExecutionChain: 来收集请求对应的拦截器，如果是匹配型的就进行请求匹配，否自直接添加到处理链中</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> HandlerExecutionChain <span class="title">getHandlerExecutionChain</span><span class="params">(Object handler, HttpServletRequest request)</span> </span>&#123;</span><br><span class="line">   HandlerExecutionChain chain = (handler <span class="keyword">instanceof</span> HandlerExecutionChain ?</span><br><span class="line">         (HandlerExecutionChain) handler : <span class="keyword">new</span> HandlerExecutionChain(handler));</span><br><span class="line">   String lookupPath = <span class="keyword">this</span>.urlPathHelper.getLookupPathForRequest(request, LOOKUP_PATH);</span><br><span class="line">   <span class="keyword">for</span> (HandlerInterceptor interceptor : <span class="keyword">this</span>.adaptedInterceptors) &#123;</span><br><span class="line">      <span class="keyword">if</span> (interceptor <span class="keyword">instanceof</span> MappedInterceptor) &#123;</span><br><span class="line">         MappedInterceptor mappedInterceptor = (MappedInterceptor) interceptor;</span><br><span class="line">          <span class="comment">//如果是匹配型就先匹配</span></span><br><span class="line">         <span class="keyword">if</span> (mappedInterceptor.matches(lookupPath, <span class="keyword">this</span>.pathMatcher)) &#123;</span><br><span class="line">            chain.addInterceptor(mappedInterceptor.getInterceptor());</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">           <span class="comment">//否自直接加入到处理器链</span></span><br><span class="line">         chain.addInterceptor(interceptor);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> chain;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>向容器中注册拦截器 xml方式：</li>
</ul>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">mvc:interceptors</span>&gt;</span></span><br><span class="line">   <span class="comment">&lt;!--匹配性--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:interceptor</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">mvc:mapping</span> <span class="attr">path</span>=<span class="string">&quot;/ag/*&quot;</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;com.ryx.credit.cms.intercept.AgentConfirmInterceptor&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">mvc:interceptor</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--非匹配型--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;localeChangeInterceptor&quot;</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.i18n.LocaleChangeInterceptor&quot;</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mvc:interceptors</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>WebMvcConfigurer 方式：</li>
</ul>
 <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addInterceptors</span><span class="params">(InterceptorRegistry registry)</span> </span>&#123;</span><br><span class="line">     <span class="comment">//添加注册器 InterceptorRegistration</span></span><br><span class="line">     registry.addInterceptor(<span class="keyword">new</span> HandlerInterceptor() &#123;</span><br><span class="line">         <span class="meta">@Override</span></span><br><span class="line">         <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception </span>&#123; <span class="keyword">return</span> <span class="keyword">true</span>;&#125;</span><br><span class="line"> </span><br><span class="line">         <span class="meta">@Override</span></span><br><span class="line">         <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">postHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler, ModelAndView modelAndView)</span> <span class="keyword">throws</span> Exception </span>&#123;&#125;</span><br><span class="line"> </span><br><span class="line">         <span class="meta">@Override</span></span><br><span class="line">         <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">afterCompletion</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex)</span> <span class="keyword">throws</span> Exception </span>&#123;&#125;</span><br><span class="line">     &#125;)    </span><br><span class="line">     .addPathPatterns(<span class="string">&quot;/**&quot;</span>)</span><br><span class="line">     .excludePathPatterns(<span class="string">&quot;/a&quot;</span>);</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//InterceptorRegistration</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> Object <span class="title">getInterceptor</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">//如果未添加匹配路劲也未添加排除匹配就默认为非匹配型拦截器</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.includePatterns.isEmpty() &amp;&amp; <span class="keyword">this</span>.excludePatterns.isEmpty()) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.interceptor;</span><br><span class="line">    &#125;</span><br><span class="line">    String[] include = StringUtils.toStringArray(<span class="keyword">this</span>.includePatterns);</span><br><span class="line">    String[] exclude = StringUtils.toStringArray(<span class="keyword">this</span>.excludePatterns);</span><br><span class="line">    MappedInterceptor mappedInterceptor = <span class="keyword">new</span> MappedInterceptor(include, exclude, <span class="keyword">this</span>.interceptor);</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.pathMatcher != <span class="keyword">null</span>) &#123;</span><br><span class="line">        mappedInterceptor.setPathMatcher(<span class="keyword">this</span>.pathMatcher);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> mappedInterceptor;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="请求映射到具体的处理程序关系初始化"><a href="#请求映射到具体的处理程序关系初始化" class="headerlink" title="请求映射到具体的处理程序关系初始化"></a>请求映射到具体的处理程序关系初始化</h1><h2 id="如何注册请求处理器"><a href="#如何注册请求处理器" class="headerlink" title="如何注册请求处理器"></a>如何注册请求处理器</h2><ul>
<li>被@Controller或者@RequestMapping 注解标注的容器Bean的内部 被@RequestMapping注解标注方法 并构建成<u>HandlerMethod</u>（class,method）</li>
<li>以”/“开头的或者别名是以”/“开头的容器内的任意Bean (<u>Controller HandlerFunction Servlet HttpRequestHandler</u>)</li>
</ul>
<h2 id="AbstractHandlerMapping-有两个子类AbstractUrlHandlerMapping-和-AbstractHandlerMethodMapping"><a href="#AbstractHandlerMapping-有两个子类AbstractUrlHandlerMapping-和-AbstractHandlerMethodMapping" class="headerlink" title="AbstractHandlerMapping 有两个子类AbstractUrlHandlerMapping 和 AbstractHandlerMethodMapping"></a>AbstractHandlerMapping 有两个子类AbstractUrlHandlerMapping 和 AbstractHandlerMethodMapping</h2><ul>
<li><p>其中AbstractUrlHandlerMapping  维护了 <strong>Map&lt;String, Object&gt; handlerMap</strong> 映射关系并提供注册映射关系的方法，具体的注册逻辑交给了子类来处理，</p>
<p>子类的注册时机是在AbstractHandlerMapping 中的 已实现了的initApplicationContext 中，子类中调用父类的initApplicationContext 后初始化映射</p>
</li>
<li><p>其中AbstractHandlerMethodMapping 维护了 <strong>MappingRegistry mappingRegistry</strong> 映射关系并提供注册映射关系的方法，并实现了注册逻辑的模板反方，并预留了是否需要注册的接口留给了子类去实现</p>
</li>
</ul>
<p>AbstractHandlerMethodMapping 注册的模板方法 是在InitializingBean   afterPropertiesSet() 接口方法中实现的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">afterPropertiesSet</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	initHandlerMethods();</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">initHandlerMethods</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="comment">//获取容器中候选的beanName</span></span><br><span class="line">    <span class="keyword">for</span> (String beanName : getCandidateBeanNames()) &#123;</span><br><span class="line">    </span><br><span class="line">		<span class="comment">//排除scopedTarget.</span></span><br><span class="line">        <span class="keyword">if</span> (!beanName.startsWith(SCOPED_TARGET_NAME_PREFIX)) &#123;     </span><br><span class="line">			<span class="comment">//注册        </span></span><br><span class="line">        	processCandidateBean(beanName);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    handlerMethodsInitialized(getHandlerMethods());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">processCandidateBean</span><span class="params">(String beanName)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line"> 	Class&lt;?&gt; beanType = beanType = obtainApplicationContext().getType(beanName);  </span><br><span class="line">    </span><br><span class="line">	<span class="comment">//isHandler 交给了子类去实现</span></span><br><span class="line">    <span class="keyword">if</span> (beanType != <span class="keyword">null</span> &amp;&amp; isHandler(beanType)) &#123;</span><br><span class="line">    </span><br><span class="line">	<span class="comment">//探测bean内部那些方法是handler,</span></span><br><span class="line">     detectHandlerMethods(beanName);</span><br><span class="line">     </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>





<h2 id="AbstractHandlerMethodMapping子类RequestMappingHandlerMapping"><a href="#AbstractHandlerMethodMapping子类RequestMappingHandlerMapping" class="headerlink" title="AbstractHandlerMethodMapping子类RequestMappingHandlerMapping"></a>AbstractHandlerMethodMapping子类RequestMappingHandlerMapping</h2><ul>
<li>RequestMappingHandlerMapping映射那些被 Controller 或者RequestMapping 注解的类 ,并交由AbstractHandlerMethodMapping  来解析这些类下的方法为handler，并注册到 mappingRegistry</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">isHandler</span><span class="params">(Class&lt;?&gt; beanType)</span> </span>&#123;</span><br><span class="line">   <span class="keyword">return</span> (AnnotatedElementUtils.hasAnnotation(beanType, Controller.class) ||</span><br><span class="line">         AnnotatedElementUtils.hasAnnotation(beanType, RequestMapping.class));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>并且解析Method  RequestMappingInfo</li>
</ul>
<h2 id="AbstractUrlHandlerMapping"><a href="#AbstractUrlHandlerMapping" class="headerlink" title="AbstractUrlHandlerMapping"></a>AbstractUrlHandlerMapping</h2><p>AbstractUrlHandlerMapping   缓存了映射关系并提供了请求与处理器注册的方法registerHandler ，并且注册处理器过程中设置ROOThandler 和默认的 defaultHandler</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">          <span class="keyword">if</span> (urlPath.equals(<span class="string">&quot;/&quot;</span>)) &#123;</span><br><span class="line">	setRootHandler(resolvedHandler);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (urlPath.equals(<span class="string">&quot;/*&quot;</span>)) &#123;</span><br><span class="line">	setDefaultHandler(resolvedHandler);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">	<span class="keyword">this</span>.handlerMap.put(urlPath, resolvedHandler);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="AbstractUrlHandlerMapping-子类AbstractDetectingUrlHandlerMapping"><a href="#AbstractUrlHandlerMapping-子类AbstractDetectingUrlHandlerMapping" class="headerlink" title="AbstractUrlHandlerMapping  子类AbstractDetectingUrlHandlerMapping"></a>AbstractUrlHandlerMapping  子类AbstractDetectingUrlHandlerMapping</h3><p>AbstractDetectingUrlHandlerMapping 在探测处理器过程中，定义了探测的源也就是容器内的bean，并预留了抽象方法 String[] determineUrlsForHandler(String beanName) 来确定bean名称是否可作为handler   ,</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">initApplicationContext</span><span class="params">()</span> <span class="keyword">throws</span> ApplicationContextException </span>&#123;</span><br><span class="line">		<span class="keyword">super</span>.initApplicationContext();</span><br><span class="line">		detectHandlers();</span><br><span class="line">	&#125;</span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">detectHandlers</span><span class="params">()</span> <span class="keyword">throws</span> BeansException </span>&#123;</span><br><span class="line">    ApplicationContext applicationContext = obtainApplicationContext();</span><br><span class="line">    String[] beanNames = (<span class="keyword">this</span>.detectHandlersInAncestorContexts ?</span><br><span class="line">    BeanFactoryUtils.beanNamesForTypeIncludingAncestors(applicationContext, Object.class) :</span><br><span class="line">    applicationContext.getBeanNamesForType(Object.class));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (String beanName : beanNames) &#123;</span><br><span class="line">    </span><br><span class="line">        <span class="comment">/*********determineUrlsForHandler 方法为抽象方法，子类觉得bean的是否可以作为Handler来注册******/</span></span><br><span class="line">        String[] urls = determineUrlsForHandler(beanName);</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span> (!ObjectUtils.isEmpty(urls)) &#123;</span><br><span class="line">        registerHandler(urls, beanName);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中有一个默认的实现类 BeanNameUrlHandlerMapping，BeanNameUrlHandlerMapping 的 方法定义了那些bean的名字可以被注册映射关系，那些以 “/”  开始 或者别名以 ‘/“ 开始的都可以注册为一个请求和处理的映射</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> String[] determineUrlsForHandler(String beanName) &#123;</span><br><span class="line">		List&lt;String&gt; urls = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">		<span class="keyword">if</span> (beanName.startsWith(<span class="string">&quot;/&quot;</span>)) &#123;</span><br><span class="line">			urls.add(beanName);</span><br><span class="line">		&#125;</span><br><span class="line">		String[] aliases = obtainApplicationContext().getAliases(beanName);</span><br><span class="line">		<span class="keyword">for</span> (String alias : aliases) &#123;</span><br><span class="line">			<span class="keyword">if</span> (alias.startsWith(<span class="string">&quot;/&quot;</span>)) &#123;</span><br><span class="line">				urls.add(alias);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> StringUtils.toStringArray(urls);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h3 id="AbstractUrlHandlerMapping-子类SimpleUrlHandlerMapping"><a href="#AbstractUrlHandlerMapping-子类SimpleUrlHandlerMapping" class="headerlink" title="AbstractUrlHandlerMapping  子类SimpleUrlHandlerMapping"></a>AbstractUrlHandlerMapping  子类SimpleUrlHandlerMapping</h3><p>SimpleUrlHandlerMapping 维护这一个url与处理器的映射关系  Map&lt;String, Object&gt; urlMap</p>
<p>我们常用的ResourceHandler 就采用了这个HandlerMapping，其中handler 采用的是HttpRequestHandler   , ResourceHandlerRegistry内部分代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> AbstractHandlerMapping <span class="title">getHandlerMapping</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.registrations.isEmpty()) &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		Map&lt;String, HttpRequestHandler&gt; urlMap = <span class="keyword">new</span> LinkedHashMap&lt;&gt;();</span><br><span class="line">		<span class="keyword">for</span> (ResourceHandlerRegistration registration : <span class="keyword">this</span>.registrations) &#123;</span><br><span class="line">			<span class="keyword">for</span> (String pathPattern : registration.getPathPatterns()) &#123;</span><br><span class="line">				ResourceHttpRequestHandler handler = registration.getRequestHandler();</span><br><span class="line">				<span class="keyword">if</span> (<span class="keyword">this</span>.pathHelper != <span class="keyword">null</span>) &#123;</span><br><span class="line">					handler.setUrlPathHelper(<span class="keyword">this</span>.pathHelper);</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">if</span> (<span class="keyword">this</span>.contentNegotiationManager != <span class="keyword">null</span>) &#123;</span><br><span class="line">					handler.setContentNegotiationManager(<span class="keyword">this</span>.contentNegotiationManager);</span><br><span class="line">				&#125;</span><br><span class="line">				handler.setServletContext(<span class="keyword">this</span>.servletContext);</span><br><span class="line">				handler.setApplicationContext(<span class="keyword">this</span>.applicationContext);</span><br><span class="line">				<span class="keyword">try</span> &#123;</span><br><span class="line">					handler.afterPropertiesSet();</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">					<span class="keyword">throw</span> <span class="keyword">new</span> BeanInitializationException(<span class="string">&quot;Failed to init ResourceHttpRequestHandler&quot;</span>, ex);</span><br><span class="line">				&#125;</span><br><span class="line">				</span><br><span class="line">				<span class="comment">//将资源映射到处理器</span></span><br><span class="line">				urlMap.put(pathPattern, handler);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">//构建资源映射处理器</span></span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> SimpleUrlHandlerMapping(urlMap, <span class="keyword">this</span>.order);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h3 id="AbstractUrlHandlerMapping-子类WelcomePageHandlerMapping"><a href="#AbstractUrlHandlerMapping-子类WelcomePageHandlerMapping" class="headerlink" title="AbstractUrlHandlerMapping  子类WelcomePageHandlerMapping"></a>AbstractUrlHandlerMapping  子类WelcomePageHandlerMapping</h3><p>WelcomePageHandlerMapping 未做任何映射，只是设置了AbstractUrlHandlerMapping   “/“   RootHandler </p>
<h3 id="Handler处理程序"><a href="#Handler处理程序" class="headerlink" title="Handler处理程序"></a>Handler处理程序</h3><ol>
<li><p>Controller </p>
<p>执行适配程序 SimpleControllerHandlerAdapter</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Controller</span> </span>&#123;</span><br><span class="line">   <span class="meta">@Nullable</span></span><br><span class="line">   <span class="function">ModelAndView <span class="title">handleRequest</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> Exception</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleControllerHandlerAdapter</span> <span class="keyword">implements</span> <span class="title">HandlerAdapter</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">supports</span><span class="params">(Object handler)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> (handler <span class="keyword">instanceof</span> Controller);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> ModelAndView <span class="title">handle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span></span></span><br><span class="line"><span class="function">			<span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">         <span class="comment">//返回试图</span></span><br><span class="line">		<span class="keyword">return</span> ((Controller) handler).handleRequest(request, response);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	...</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>HandlerFunction (Flux)</p>
<p>执行适配程序 HandlerFunctionAdapter</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">HandlerFunction</span>&lt;<span class="title">T</span> <span class="keyword">extends</span> <span class="title">ServerResponse</span>&gt; </span>&#123;</span><br><span class="line">   <span class="function">T <span class="title">handle</span><span class="params">(ServerRequest request)</span> <span class="keyword">throws</span> Exception</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>Servlet </p>
<p>执行适配程序SimpleServletHandlerAdapter</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleServletHandlerAdapter</span> <span class="keyword">implements</span> <span class="title">HandlerAdapter</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="meta">@Override</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">supports</span><span class="params">(Object handler)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">return</span> (handler <span class="keyword">instanceof</span> Servlet);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="meta">@Override</span></span><br><span class="line">   <span class="meta">@Nullable</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> ModelAndView <span class="title">handle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span></span></span><br><span class="line"><span class="function">         <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">       <span class="comment">//未返回试图</span></span><br><span class="line">      ((Servlet) handler).service(request, response);</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"> ...</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>HttpRequestHandler</p>
<p>执行适配程序 HttpRequestHandlerAdapter</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HttpRequestHandlerAdapter</span> <span class="keyword">implements</span> <span class="title">HandlerAdapter</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="meta">@Override</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">supports</span><span class="params">(Object handler)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">return</span> (handler <span class="keyword">instanceof</span> HttpRequestHandler);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="meta">@Override</span></span><br><span class="line">   <span class="meta">@Nullable</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> ModelAndView <span class="title">handle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span></span></span><br><span class="line"><span class="function">         <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">      <span class="comment">//未返回试图</span></span><br><span class="line">      ((HttpRequestHandler) handler).handleRequest(request, response);</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   ...</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>HandlerMethod</p>
</li>
</ol>
<h1 id="HandlerMapping接口方法getHandler"><a href="#HandlerMapping接口方法getHandler" class="headerlink" title="HandlerMapping接口方法getHandler"></a>HandlerMapping接口方法getHandler</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> HandlerExecutionChain <span class="title">getHandler</span><span class="params">(HttpServletRequest request)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">   <span class="comment">//子类实现方法-通过缓存的映射关系，获取具体的处理器 Handler</span></span><br><span class="line">   Object handler = getHandlerInternal(request);</span><br><span class="line">   <span class="comment">//获取默认的处理器</span></span><br><span class="line">   <span class="keyword">if</span> (handler == <span class="keyword">null</span>) &#123;</span><br><span class="line">      handler = getDefaultHandler();</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">if</span> (handler == <span class="keyword">null</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="comment">//如果是String类型从容器中获取bean</span></span><br><span class="line">   <span class="keyword">if</span> (handler <span class="keyword">instanceof</span> String) &#123;</span><br><span class="line">      String handlerName = (String) handler;</span><br><span class="line">      handler = obtainApplicationContext().getBean(handlerName);</span><br><span class="line">   &#125;</span><br><span class="line">   </span><br><span class="line">   <span class="comment">/******构建HandlerExecutionChain,放入处理器，并根据请求url，匹配填充MappedInterceptor 列表******/</span></span><br><span class="line">   HandlerExecutionChain executionChain = getHandlerExecutionChain(handler, request);</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (hasCorsConfigurationSource(handler) || CorsUtils.isPreFlightRequest(request)) &#123;</span><br><span class="line">      CorsConfiguration config = (<span class="keyword">this</span>.corsConfigurationSource != <span class="keyword">null</span> ? <span class="keyword">this</span>.corsConfigurationSource.getCorsConfiguration(request) : <span class="keyword">null</span>);</span><br><span class="line">      CorsConfiguration handlerConfig = getCorsConfiguration(handler, request);</span><br><span class="line">      config = (config != <span class="keyword">null</span> ? config.combine(handlerConfig) : handlerConfig);</span><br><span class="line">      executionChain = getCorsHandlerExecutionChain(request, executionChain, config);</span><br><span class="line">   &#125;</span><br><span class="line"><span class="comment">/******返回处理器链，内部包含了Handelr 和 匹配的拦截器列表******/</span></span><br><span class="line">   <span class="keyword">return</span> executionChain;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> HandlerExecutionChain <span class="title">getHandlerExecutionChain</span><span class="params">(Object handler, HttpServletRequest request)</span> </span>&#123;</span><br><span class="line">   HandlerExecutionChain chain = (handler <span class="keyword">instanceof</span> HandlerExecutionChain ?</span><br><span class="line">         (HandlerExecutionChain) handler : <span class="keyword">new</span> HandlerExecutionChain(handler));</span><br><span class="line"></span><br><span class="line">   String lookupPath = <span class="keyword">this</span>.urlPathHelper.getLookupPathForRequest(request, LOOKUP_PATH);</span><br><span class="line">   <span class="keyword">for</span> (HandlerInterceptor interceptor : <span class="keyword">this</span>.adaptedInterceptors) &#123;</span><br><span class="line">      <span class="keyword">if</span> (interceptor <span class="keyword">instanceof</span> MappedInterceptor) &#123;</span><br><span class="line">         MappedInterceptor mappedInterceptor = (MappedInterceptor) interceptor;</span><br><span class="line">         <span class="comment">/******填充匹配的拦截器列表******/</span></span><br><span class="line">         <span class="keyword">if</span> (mappedInterceptor.matches(lookupPath, <span class="keyword">this</span>.pathMatcher)) &#123;</span><br><span class="line">            chain.addInterceptor(mappedInterceptor.getInterceptor());</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">         chain.addInterceptor(interceptor);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> chain;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>springmvc</tag>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringWebMvc-HandlerAdapter</title>
    <url>/2021/09/08/spring/HandlerAdapter/</url>
    <content><![CDATA[<p>HandlerAdapter负责调用映射到请求的处理程序，HandlerAdapter主要目的是隐藏调用细节，简化DispatcherServlet，让DispatcherServlet专注于流程控制.</p>
<p>DispatcherServlet在处理请求的时候从维护的handler适配器列表中获取并选择合适的适配器执行器去执行，那HandlerAdapter是如何注册到DispatcherServlet中的呢 </p>
<p>HandlerMapping中映射着具体的处理器handler类型繁多（如HandlerMethod Controller HandlerFunction Servlet HttpRequestHandler等），功能各异，那这些处理程序的执行各有不同，执行方式也各有不同，DispatcherServlet是如何根据handler找到指定的HandlerAdapter并完成执行请求的呢</p>
<p>HandlerAdapter要处理的具体执行器有多重类型（如HandlerMethod Controller HandlerFunction Servlet HttpRequestHandler等）那么他们是通过哪些HandlerAdapter具体实现来处理的呢 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">HandlerAdapter</span> </span>&#123;</span><br><span class="line">    <span class="comment">/******支持处理程序检测******/</span></span><br><span class="line">	<span class="function"><span class="keyword">boolean</span> <span class="title">supports</span><span class="params">(Object handler)</span></span>;</span><br><span class="line">    <span class="comment">/******处理请求调用******/</span></span><br><span class="line">	<span class="function">ModelAndView <span class="title">handle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception</span>;</span><br><span class="line">    <span class="comment">/******返回最后修改时间******/</span></span><br><span class="line">	<span class="function"><span class="keyword">long</span> <span class="title">getLastModified</span><span class="params">(HttpServletRequest request, Object handler)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="HandlerAdapter探测"><a href="#HandlerAdapter探测" class="headerlink" title="HandlerAdapter探测"></a>HandlerAdapter探测</h1><p>DispatcherServlet 继承自FrameworkServlet ，并且实现了onRefresh 监听WebApplicationContext刷新方法，完成MVC各个处理节点处理策略的初始化，其中HandlerAdapter就是其中之一，</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onRefresh</span><span class="params">(ApplicationContext context)</span> </span>&#123;</span><br><span class="line">		initStrategies(context);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">initStrategies</span><span class="params">(ApplicationContext context)</span> </span>&#123;</span><br><span class="line">    initMultipartResolver(context);</span><br><span class="line">    initLocaleResolver(context);</span><br><span class="line">    initThemeResolver(context);</span><br><span class="line">    initHandlerMappings(context);</span><br><span class="line">    initHandlerAdapters(context);</span><br><span class="line">    initHandlerExceptionResolvers(context);</span><br><span class="line">    initRequestToViewNameTranslator(context);</span><br><span class="line">    initViewResolvers(context);</span><br><span class="line">    initFlashMapManager(context);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>完成探测并维护HandlerAdapter到DispatcherServlet的handlerAdapters中：</p>
<ol>
<li>根据detectAllHandlerAdapters（是否探测所有的HandlerAdapter）属性 true 完成 从bean容器中获取所有的HandlerAdapter</li>
<li>根据detectAllHandlerAdapters（是否探测所有的HandlerAdapter）属性 false 完成 从bean容器中获取beanName 为handlerAdapter类型为HandlerAdapter</li>
<li>如果都没有，就从默认DispatcherServlet.properties 配置中获取，spring-webmvc 包下配置了默认的策略规则HandlerAdapter的默认策略规则为</li>
</ol>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">org.springframework.web.servlet.HandlerAdapter</span>=<span class="string">org.springframework.web.servlet.mvc.HttpRequestHandlerAdapter,\</span></span><br><span class="line"><span class="string">   org.springframework.web.servlet.mvc.SimpleControllerHandlerAdapter,\</span></span><br><span class="line"><span class="string">   org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter,\</span></span><br><span class="line"><span class="string">   org.springframework.web.servlet.function.support.HandlerFunctionAdapter</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">initHandlerAdapters</span><span class="params">(ApplicationContext context)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>.handlerAdapters = <span class="keyword">null</span>;</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.detectAllHandlerAdapters) &#123;</span><br><span class="line">			Map&lt;String, HandlerAdapter&gt; matchingBeans =</span><br><span class="line">					BeanFactoryUtils.beansOfTypeIncludingAncestors(context, HandlerAdapter.class, <span class="keyword">true</span>, <span class="keyword">false</span>);</span><br><span class="line">			<span class="keyword">if</span> (!matchingBeans.isEmpty()) &#123;</span><br><span class="line">				<span class="keyword">this</span>.handlerAdapters = <span class="keyword">new</span> ArrayList&lt;&gt;(matchingBeans.values());</span><br><span class="line">				AnnotationAwareOrderComparator.sort(<span class="keyword">this</span>.handlerAdapters);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">				HandlerAdapter ha = context.getBean(HANDLER_ADAPTER_BEAN_NAME, HandlerAdapter.class);</span><br><span class="line">				<span class="keyword">this</span>.handlerAdapters = Collections.singletonList(ha);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.handlerAdapters == <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">this</span>.handlerAdapters = getDefaultStrategies(context, HandlerAdapter.class);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h1 id="DispatcherServlet根据handler匹配到HandlerAdapter并完成请求执行"><a href="#DispatcherServlet根据handler匹配到HandlerAdapter并完成请求执行" class="headerlink" title="DispatcherServlet根据handler匹配到HandlerAdapter并完成请求执行"></a>DispatcherServlet根据handler匹配到HandlerAdapter并完成请求执行</h1><p>DispatcherServlet根据请求完成HandlerExecutionChain构建之后，DispatcherServlet已经知道了需要哪个执行器去执行并且需要执行哪些拦截器，剩下的需要Adapter接管具体的Handler执行操作。</p>
<p>程序从Mapping阶段 （根据请求映射到确定handler和HandlerInterceptor）进入Adapter阶段(根据具体的handler确定具体的适配执行器),在适配阶段，DispatcherServlet根据具体的Handler由HandlerAdapter 自行适配handler。并告知DispatcherServlet，是否能处理，如果HandlerAdapter 告知DispatcherServlet支持此当前请求的handler，那么DispatcherServlet将采用，并在后续的方法中将执行操作委派给HandlerAdapter </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">HandlerAdapter ha = getHandlerAdapter(mappedHandler.getHandler());</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> HandlerAdapter <span class="title">getHandlerAdapter</span><span class="params">(Object handler)</span> <span class="keyword">throws</span> ServletException </span>&#123;</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.handlerAdapters != <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">for</span> (HandlerAdapter adapter : <span class="keyword">this</span>.handlerAdapters) &#123;</span><br><span class="line">				<span class="keyword">if</span> (adapter.supports(handler)) &#123;</span><br><span class="line">					<span class="keyword">return</span> adapter;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/******根据具体的handler确定具体的适配执行器******/</span></span><br><span class="line">HandlerAdapter ha = getHandlerAdapter(mappedHandler.getHandler());</span><br><span class="line"><span class="comment">/******拦截器PreHandle ******/</span></span><br><span class="line"><span class="keyword">if</span> (!handlerExecutionChain.applyPreHandle(processedRequest, response)) &#123;<span class="keyword">return</span>;&#125;</span><br><span class="line"><span class="comment">/******将执行操作委派给HandlerAdapter ******/</span></span><br><span class="line">mv = ha.handle(processedRequest, response, mappedHandler.getHandler());</span><br><span class="line"><span class="comment">/******拦截器PostHandle ******/</span></span><br><span class="line">handlerExecutionChain.applyPostHandle(processedRequest, response, mv);</span><br></pre></td></tr></table></figure>

<h1 id="处理器Handler类型及与之对应的HandlerAdapter"><a href="#处理器Handler类型及与之对应的HandlerAdapter" class="headerlink" title="处理器Handler类型及与之对应的HandlerAdapter"></a>处理器Handler类型及与之对应的HandlerAdapter</h1><img src="/2021/09/08/spring/HandlerAdapter/HandlerAdapterClassH.png" class="" title="HandlerAdapter结构图">

<h2 id="Handler类型"><a href="#Handler类型" class="headerlink" title="Handler类型"></a>Handler类型</h2><ol>
<li><p>Handler类型org.springframework.web.method.HandlerMethod对应AbstractHandlerMethodAdapter (RequestMappingHandlerAdapter)</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractHandlerMethodAdapter</span> <span class="keyword">extends</span> <span class="title">WebContentGenerator</span> <span class="keyword">implements</span> <span class="title">HandlerAdapter</span>, <span class="title">Ordered</span> </span>&#123;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">supports</span><span class="params">(Object handler)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> (handler <span class="keyword">instanceof</span> HandlerMethod &amp;&amp; supportsInternal((HandlerMethod) handler));</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>Handler类型 org.springframework.web.servlet.mvc.Controller 对应 SimpleControllerHandlerAdapter</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleControllerHandlerAdapter</span> <span class="keyword">implements</span> <span class="title">HandlerAdapter</span> </span>&#123;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">supports</span><span class="params">(Object handler)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> (handler <span class="keyword">instanceof</span> Controller);</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> ModelAndView <span class="title">handle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span><span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> ((Controller) handler).handleRequest(request, response);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>Handler类型 org.springframework.web.servlet.function.HandlerFunction  对应 HandlerFunctionAdapter</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HandlerFunctionAdapter</span> <span class="keyword">implements</span> <span class="title">HandlerAdapter</span>, <span class="title">Ordered</span> </span>&#123;</span><br><span class="line">     <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">supports</span><span class="params">(Object handler)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> handler <span class="keyword">instanceof</span> HandlerFunction;</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> ModelAndView <span class="title">handle</span><span class="params">(HttpServletRequest servletRequest,HttpServletResponse servletResponse,Object handler)</span> <span class="keyword">throws</span></span></span><br><span class="line"><span class="function">        Exception </span>&#123;</span><br><span class="line">		HandlerFunction&lt;?&gt; handlerFunction = (HandlerFunction&lt;?&gt;) handler;</span><br><span class="line">		ServerRequest serverRequest = getServerRequest(servletRequest);</span><br><span class="line">		ServerResponse serverResponse = handlerFunction.handle(serverRequest);</span><br><span class="line">		<span class="keyword">return</span> serverResponse.writeTo(servletRequest, servletResponse,<span class="keyword">new</span> ServerRequestContext(serverRequest));</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line">	</span><br></pre></td></tr></table></figure>

<p>子类</p>
<ul>
<li>ResourceHandlerFunction，资源文件处理器</li>
</ul>
</li>
<li><p>Handler类型 javax.servlet.Servlet.Servlet  对应 SimpleServletHandlerAdapter</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleServletHandlerAdapter</span> <span class="keyword">implements</span> <span class="title">HandlerAdapter</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">supports</span><span class="params">(Object handler)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> (handler <span class="keyword">instanceof</span> Servlet);</span><br><span class="line">	&#125;  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> ModelAndView <span class="title">handle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span><span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">		((Servlet) handler).service(request, response);</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>Handler类型 org.springframework.web.servlet.mvc.HttpRequestHandler 对应 HttpRequestHandlerAdapter</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HttpRequestHandlerAdapter</span> <span class="keyword">implements</span> <span class="title">HandlerAdapter</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">supports</span><span class="params">(Object handler)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> (handler <span class="keyword">instanceof</span> HttpRequestHandler);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> ModelAndView <span class="title">handle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span><span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">		((HttpRequestHandler) handler).handleRequest(request, response);</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  子类</p>
<ul>
<li><p>ResourceHttpRequestHandler</p>
</li>
<li><p>DefaultServletHttpRequestHandler </p>
</li>
<li><p>HttpInvokerServiceExporter </p>
</li>
<li><p>HessianServiceExporter</p>
</li>
</ul>
</li>
</ol>
<h1 id="AbstractHandlerMethodAdapter如何执行"><a href="#AbstractHandlerMethodAdapter如何执行" class="headerlink" title="AbstractHandlerMethodAdapter如何执行"></a>AbstractHandlerMethodAdapter如何执行</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> ModelAndView <span class="title">handleInternal</span><span class="params">(HttpServletRequest request,HttpServletResponse response, HandlerMethod handlerMethod)</span> <span class="keyword">throws</span></span></span><br><span class="line"><span class="function">Exception </span>&#123;</span><br><span class="line">		ModelAndView mav;</span><br><span class="line">		checkRequest(request);</span><br><span class="line">		mav = invokeHandlerMethod(request, response, handlerMethod);</span><br><span class="line">		<span class="keyword">return</span> mav;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">protected</span> ModelAndView <span class="title">invokeHandlerMethod</span><span class="params">(HttpServletRequest request,HttpServletResponse response, HandlerMethod handlerMethod)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line"></span><br><span class="line">		ServletWebRequest webRequest = <span class="keyword">new</span> ServletWebRequest(request, response);</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			WebDataBinderFactory binderFactory = getDataBinderFactory(handlerMethod);</span><br><span class="line">			ModelFactory modelFactory = getModelFactory(handlerMethod, binderFactory);</span><br><span class="line"></span><br><span class="line">			ServletInvocableHandlerMethod invocableMethod = createInvocableHandlerMethod(handlerMethod);</span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">this</span>.argumentResolvers != <span class="keyword">null</span>) &#123;</span><br><span class="line">				invocableMethod.setHandlerMethodArgumentResolvers(<span class="keyword">this</span>.argumentResolvers);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">this</span>.returnValueHandlers != <span class="keyword">null</span>) &#123;</span><br><span class="line">				invocableMethod.setHandlerMethodReturnValueHandlers(<span class="keyword">this</span>.returnValueHandlers);</span><br><span class="line">			&#125;</span><br><span class="line">			invocableMethod.setDataBinderFactory(binderFactory);</span><br><span class="line">			invocableMethod.setParameterNameDiscoverer(<span class="keyword">this</span>.parameterNameDiscoverer);</span><br><span class="line"></span><br><span class="line">			ModelAndViewContainer mavContainer = <span class="keyword">new</span> ModelAndViewContainer();</span><br><span class="line">			mavContainer.addAllAttributes(RequestContextUtils.getInputFlashMap(request));</span><br><span class="line">			modelFactory.initModel(webRequest, mavContainer, invocableMethod);</span><br><span class="line">			mavContainer.setIgnoreDefaultModelOnRedirect(<span class="keyword">this</span>.ignoreDefaultModelOnRedirect);</span><br><span class="line"></span><br><span class="line">			invocableMethod.invokeAndHandle(webRequest, mavContainer);</span><br><span class="line">			<span class="keyword">if</span> (asyncManager.isConcurrentHandlingStarted()) &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="keyword">return</span> getModelAndView(mavContainer, modelFactory, webRequest);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">finally</span> &#123;</span><br><span class="line">			webRequest.requestCompleted();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>



<h2 id="HandlerMethodArgumentResolver"><a href="#HandlerMethodArgumentResolver" class="headerlink" title="HandlerMethodArgumentResolver"></a>HandlerMethodArgumentResolver</h2><p>对HandlerMethod对应方法中的参数进行逐个的解析，并赋值</p>
<h2 id="HandlerMethodReturnValueHandler"><a href="#HandlerMethodReturnValueHandler" class="headerlink" title="HandlerMethodReturnValueHandler"></a>HandlerMethodReturnValueHandler</h2><p>对返回的结果进行处理</p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>springmvc</tag>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>面向对象设计中的要素与原则有哪些</title>
    <url>/2021/09/13/java/Designprinciples/</url>
    <content><![CDATA[<p>面向对象是基于万物皆是对象这个哲学观点，把一个对象抽象成类，具体就是把一个对象的静态特这和动态特征抽象成属性和方法，也就是把一类事物的数据结构及算法封装在一个类之中，程序是多个对象和互相之间的通信组成的。</p>
<h1 id="面向对象设计的三个基本要素"><a href="#面向对象设计的三个基本要素" class="headerlink" title="面向对象设计的三个基本要素"></a>面向对象设计的三个基本要素</h1><img src="/2021/09/13/java/Designprinciples/58e757d32da94.png" class="" title="面向对象设计的三个基本要素">

<ul>
<li><h3 id="封装"><a href="#封装" class="headerlink" title="封装"></a>封装</h3><p>一种信息隐蔽技术，他体现于类的说明，是对象重要的特性。封装使得数据和操作数据的方法封装为一个整体，想成独立性很强的模块，使得用户只能看到对象的外部特性（对象可以接受拿些信息，可以进行何种处理），而对象的内部特性（内部私有属性和实现处理能力的算法）用户是看不到的。简而言之就是说，封装使对象的设计者与对象的使用者分开，使用者只要知道对象可以做什么就可以了，无需知道具体是怎么实现的。借助封装有助于提高类和系统的安全性。</p>
</li>
<li><h3 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h3><h4 id="继承的概述"><a href="#继承的概述" class="headerlink" title="继承的概述"></a>继承的概述</h4><p>​    继承是使用已存在的类的定义作为基础建立新类的技术，新类的定义可以增加新的数据或新的功能，也可以用父类的功能，但不能选择性地继承父类。通过使用继承我们能够非常方便地复用以前的代码，能够大大的提高开发的效率。</p>
<p>​    诚然，继承定义了类如何相互关联，共享特性。对于若干个相同或者相识的类，我们可以抽象出他们共有的行为或者属相并将其定义成一个父类或者超类，然后用这些类继承该父类，他们不仅可以拥有父类的属性、方法还可以定义自己独有的属性或者方法。</p>
<h4 id="使用继承时需要记住三句话"><a href="#使用继承时需要记住三句话" class="headerlink" title="使用继承时需要记住三句话"></a>使用继承时需要记住三句话</h4><ol>
<li><p>子类拥有父类非private的属性和方法。</p>
</li>
<li><p>子类可以拥有自己属性和方法，即子类可以对父类进行扩展。</p>
</li>
<li><p>子类可以用自己的方式实现父类的方法（重写）</p>
</li>
</ol>
<p>  学习继承一定少不了这三个东西：<em><strong>构造器、protected关键字、向上转型</strong></em></p>
<p>​    通过前面我们知道子类可以继承父类的属性和方法，除了那些private的外还有一样是子类继承不了的—构造器。对于构造器而言，它只能够被调用，而不能被继承。 调用父类的构造方法我们使用super()即可。</p>
<p>​    构建过程是从父类“向外”扩散的，也就是从父类开始向子类一级一级地完成构建。而且我们并没有显示的引用父类的构造器，这就是java的聪明之处：编译器会默认给子类调用父类的构造器。</p>
<p>​    但是，这个默认调用父类的构造器是有前提的：父类有默认构造器。如果父类没有默认构造器，我们就要必须显示的使用super()来调用父类构造器，否则编译器会报错：无法找到符合父类形式的构造器。</p>
<p>​    对于子类而已,其构造器的正确初始化是非常重要的,而且当且仅当只有一个方法可以保证这点：在构造器中调用父类构造器来完成初始化，而父类构造器具有执行父类初始化所需要的所有知识和能力。</p>
<p>​    对于继承而言，子类会默认调用父类的构造器，但是如果没有默认的父类构造器，子类必须要显示的指定父类的构造器，而且必须是在子类构造器中做的第一件事(第一行代码)。</p>
<p>​    private访问修饰符，对于封装而言，是最好的选择，但这个只是基于理想的世界，有时候我们需要这样的需求：我们需要将某些事物尽可能地对这个世界隐藏，但是仍然允许子类的成员来访问它们。这个时候就需要使用到protected。</p>
<p>​    对于protected而言，它指明就类用户而言，他是private，但是对于任何继承与此类的子类而言或者其他任何位于同一个包的类而言，他却是可以访问的。</p>
<h4 id="谨慎继承"><a href="#谨慎继承" class="headerlink" title="谨慎继承"></a><strong>谨慎继承</strong></h4><p>在这里我们需要明确，继承存在如下缺陷：</p>
<ol>
<li><p>父类变，子类就必须变。</p>
</li>
<li><p>继承破坏了封装，对于父类而言，它的实现细节对与子类来说都是透明的。</p>
</li>
<li><p>继承是一种强耦合关系。</p>
</li>
</ol>
<p>所以说当我们使用继承的时候，我们需要确信使用继承确实是有效可行的办法。那么到底要不要使用继承呢？《Think in java》中提供了解决办法：问一问自己是否需要从子类向父类进行向上转型。如果必须向上转型，则继承是必要的，但是如果不需要，则应当好好考虑自己是否需要继承。</p>
<p>一般情况下我们通过抽象的类来定义模板方法，具体执行方法交由实现类来实现，不同的实现有不同的处理算法，但是具体的执行逻辑由抽象类来控制，如ApplicationContext 中AbstractApplicationContext的refresh方法</p>
</li>
<li><h3 id="多态-多态分为编译时多态和运行时多态"><a href="#多态-多态分为编译时多态和运行时多态" class="headerlink" title="多态(多态分为编译时多态和运行时多态)"></a>多态(<strong>多态分为编译时多态和运行时多态</strong>)</h3><ol>
<li><h4 id="多态的综述"><a href="#多态的综述" class="headerlink" title="多态的综述"></a><strong>多态的综述</strong></h4></li>
</ol>
<p>​    所谓多态就是指程序中定义的引用变量所指向的具体类型和通过该引用变量发出的方法调用在编程时并不确定，而是在程序运行期间才确定，即一个引用变量倒底会指向哪个类的实例对象，该引用变量发出的方法调用到底是哪个类中实现的方法，必须在由程序运行期间才能决定。因为在程序运行时才确定具体的类，这样，不用修改源程序代码，就可以让引用变量绑定到各种不同的类实现上，从而导致该引用调用的具体方法随之改变，即不修改程序代码就可以改变程序运行时所绑定的具体代码，让程序可以选择多个运行状态，这就是多态性。</p>
<p>​    所以对于多态我们可以总结如下：</p>
<p>   指向子类的父类引用由于向上转型了，它只能访问父类中拥有的方法和属性，而对于子类中存在而父类中不存在的方法，该引用是不能使用的，尽管是重载该方法。若子类重写了父类中的某些方法，在调用该些方法的时候，必定是使用子类中定义的这些方法（动态连接、动态调用）。</p>
<p>​    对于面向对象而言，多态分为编译时多态和运行时多态。其中编程时多态是静态的，主要是指方法的重载，它是根据参数列表的不同来区分不同的函数，通过编程之后会变成两个不同的函数，在运行时谈不上多态。而运行时多态是动态的，它是通过动态绑定来实现的，也就是我们所说的多态性。</p>
<ol start="2">
<li><h4 id="多态实现的条件（继承、重写、向上转型）"><a href="#多态实现的条件（继承、重写、向上转型）" class="headerlink" title="多态实现的条件（继承、重写、向上转型）"></a><strong>多态实现的条件</strong>（<em><strong>继承、重写、向上转型</strong></em>）</h4></li>
</ol>
<p>​    继承：在多态中必须存在有继承关系的子类和父类。<br>​    重写：子类对父类中某些方法进行重新定义，在调用这些方法时就会调用子类的方法。<br>​    向上转型：在多态中需要将子类的引用赋给父类对象，只有这样该引用才能够具备技能调用父类的方法和子类的方法。</p>
<p>​    只有满足了上述三个条件，我们才能够在同一个继承结构中使用统一的逻辑实现代码处理不同的对象，从而达到执行不同的行为。</p>
<p>​    对于Java而言，它多态的实现机制遵循一个原则：当超类对象引用变量引用子类对象时，被引用对象的类型而不是引用变量的类型决定了调用谁的成员方法，但是这个被调用的方法必须是在超类中定义过的，也就是说被子类覆盖的方法。</p>
<ol start="3">
<li><h4 id="实现形式"><a href="#实现形式" class="headerlink" title="实现形式"></a><strong>实现形式</strong></h4></li>
</ol>
<p>​    在Java中有两种形式可以实现多态：继承和接口</p>
<h5 id="a-基于继承实现的多态"><a href="#a-基于继承实现的多态" class="headerlink" title="a. 基于继承实现的多态"></a><strong>a. 基于继承实现的多态</strong></h5><p>​     基于继承的实现机制主要表现在父类和继承该父类的一个或多个子类对某些方法的重写，多个子类对同一方法的重写可以表现出不同的行为,对于引用子类的父类类型，在处理该引用时，它适用于继承该父类的所有子类，子类对象的不同，对方法的实现也就不同，执行相同动作产生的行为也就不同。</p>
<h5 id="b-基于接口实现的多态"><a href="#b-基于接口实现的多态" class="headerlink" title="b. 基于接口实现的多态"></a><strong>b. 基于接口实现的多态</strong></h5><p>​    在接口的多态中，指向接口的引用必须是指定这实现了该接口的一个类的实例程序，在运行时，根据对象引用的实际类型来执行对应的方法，继承都是单继承，只能为一组相关的类提供一致的服务接口。但是接口可以是多继承多实现，它能够利用一组相关或者不相关的接口进行组合与扩充，能够对外提供一致的服务接口。所以它相对于继承来说有更好的灵活性。</p>
</li>
</ul>
<h1 id="面向对象七种设计原则"><a href="#面向对象七种设计原则" class="headerlink" title="面向对象七种设计原则"></a>面向对象七种设计原则</h1><p>一个好的系统设计应该有如下的性质</p>
<ul>
<li>可扩展性（Extensibility）：可以很容易的在系统中加入一个新的功能</li>
<li>灵活性（Flexibility）：可以很容易的实现对某个代码的修改，而不担心对其他模块产生影响</li>
<li>可插入性（Pluggability）：可以很容易的将一个类抽出去复用，或者将另一个有同样功能的接口的类加入到系统里</li>
</ul>
<p>面向对象的设计原则都是以可维护性、可复用性、扩展性为基础的，这些原则相互依赖，相互补充，遵循这些设计原则可以提高系统的复用性，扩展性，同时提高系统的可维护性。</p>
<table>
<thead>
<tr>
<th>原则</th>
<th align="left">简称</th>
<th>核心思想</th>
<th align="left">概述</th>
</tr>
</thead>
<tbody><tr>
<td>单一功能原则</td>
<td align="left">Single Responsibility Principle, SRP</td>
<td>解耦和增强内聚性（高内聚，低耦      合）</td>
<td align="left">类被修改的几率很大，因此应该专注于单一的功能。<br />如果你把多个功能放在同一个类中，功能之间就形成了关联，<br />改变其中一个功能，有可能中止另一个功能，这时就需要新一轮的测试来避免可能出现的问题</td>
</tr>
<tr>
<td>开闭原则</td>
<td align="left">Open-Closed Principle, OCP</td>
<td>对扩展开放，对修改关闭</td>
<td align="left">扩展开放：模块添加新功能，不改变原有的代码,<br />修改关闭：某模块被其他模块调用，<br />如果该模块的源代码不允许修改，则该模块修改关闭的</td>
</tr>
<tr>
<td>里氏替换原则</td>
<td align="left">Liskov Substitution Principle, LSP</td>
<td>任何父类出现的地方，子类都可以替代出现</td>
<td align="left">任何父类出现的地方，子类都可以替代出现，把基类替换成它的子类，程序将不会产生任务错误和异常，反过来则不成立，如果一个软件实体使用的是一个子类的话，那么它不一定能够使用基类</td>
</tr>
<tr>
<td>依赖倒转原则</td>
<td align="left">Dependence Inversion Principle, DIP</td>
<td>要依赖于抽象，不要依赖于具体的实现</td>
<td align="left"></td>
</tr>
<tr>
<td>接口分离原则</td>
<td align="left">Interface Segregation Principle, ISP</td>
<td>不应该强迫客户程序依赖他们不需要使用的方法</td>
<td align="left">一个接口不需要提供太多的行为，一个接口应该只提供一种对外的功能，不应该把所有的操作都封装到一个接口当中</td>
</tr>
<tr>
<td>合成复用原则</td>
<td align="left">Composite Reuse Principle, CRP</td>
<td>尽量使用对象组合，而不是继承来达到复用的目的</td>
<td align="left">继承关系是强耦合，组合关系是低耦合</td>
</tr>
<tr>
<td>迪米特原则</td>
<td align="left">Law of Demeter, LoD</td>
<td>又称最少知识原则,一个对象应当对其他对象有尽可能少的了解,不和陌生人说话</td>
<td align="left">降低各个对象之间的耦合，提高系统的可维护性</td>
</tr>
</tbody></table>
<h2 id="开闭原则："><a href="#开闭原则：" class="headerlink" title="开闭原则："></a>开闭原则：</h2><p>开闭原则对扩展开放对修改关闭，并不意味着我们的代码不能做出任何变更，开闭原则是我们开发应用程序的目标，<strong>抽象化是开闭原则实现的关键，通过接口或者抽象类来完成对修改的封闭约束，不允许出现在接口中或者抽象类中不存在的public方法，采用抽象类的具体实现或者接口多实现类以及多态性的方式来完成业务功能的扩展，以实现对功能修改的关闭，对功能实现的扩展，里氏替换原则是实现开闭原则的重要方式之一。</strong></p>
<h2 id="里氏替换原则："><a href="#里氏替换原则：" class="headerlink" title="里氏替换原则："></a>里氏替换原则：</h2><p>里氏替换原则主要是针对继承的设计原则，<strong>它为良好的继承定义了一个规范</strong>，当我们考虑使用继承的时候需要考虑里氏替换原则，并且里氏替换原则是实现开闭原则的重要方式之一</p>
<ul>
<li>子类可以实现父类的抽象方法，但不能覆盖父类的非抽象方法。</li>
<li>子类可以增加自己特有的方法。</li>
<li>当子类的方法重载父类的方法时，方法的前置条件（即方法的形参）可以放大（比父类方法的输入参数更宽松）</li>
<li>当子类的方法实现父类的抽象方法时，方法的后置条件（即方法的返回值）可以被缩小（比父类更严格）</li>
</ul>
<p><strong>如果父类的某些方法在子类中已经不能使用，则建议断开父子继承关系，采用依懒，聚合，组合关系代替继承</strong></p>
<h2 id="依懒倒置原则"><a href="#依懒倒置原则" class="headerlink" title="依懒倒置原则"></a>依懒倒置原则</h2><p>依赖倒置原则规定了我们编程的方式，要针对抽象层编程，而不是针对具体实现编程，搞层模块不应依赖底层实现模块，他们都应该依赖抽象，抽象不应该依赖袭击，细节应该依赖于抽象，换句话说，开闭原则是我们开发软件要实现的目标，依赖倒置原则规定了我们开发编程时候的开发方式。</p>
<p>依赖倒置原则的常用实现方式之一是在代码中使用抽象类，而具体的实现可使用动态的特性，在运行时根据业务数据选择不同的执行策略，这些具体实现可以放入配置文件或者容器中，具体业务动态绑定到具体的实现，也是为了实现开闭原则目标的方式。</p>
<p><strong>依懒的三种写法</strong></p>
<p>构造函数传递依懒对象，也叫构造函数注入</p>
<p>Setter方法传递依懒对象，也叫Setter依懒注入</p>
<p>接口声明依懒对象，也叫接口注入，就是在接口的方法参数中声明依懒对象。</p>
<h2 id="接口隔离原则"><a href="#接口隔离原则" class="headerlink" title="接口隔离原则"></a>接口隔离原则</h2><p>接口隔离原则要求我们不要建立功能丰富的庞大接口，使用多个专门的接口来取代一个统一的接口。<strong>每一个接口应该承担一种相对独立的角色，不多不少，不干不该干的事，该干的事都要干，在具体程序上，避免操作越界，通过接口的控制操作的界限，如使用依赖倒置原则，依赖接口进行开发，查询接口只提供查询功能，在查询逻辑中禁止修改逻辑，依赖单一功能接口可避免查询中进行修改这种越界操作提高系统安全性</strong>，接口隔离原则告诉我们：使用多个隔离的接口，比使用单个接口要好，尽量降低类之间的耦合度。</p>
<ol>
<li>一个接口就只代表一个角色，每个角色都有它特定的一个接口。 </li>
<li>接口仅仅提供客户端需要的行为，即所需的方法，客户端不需要的行为则隐藏起来，应当为客户端提供尽可能小的单独的接口，而不要提供大的总接口。</li>
<li>使用接口隔离原则拆分接口时，首先必须满足单一职责原则，将一组相关的操作定义在一个接口中，且在满足高内聚的前提下，接口中的方法越少越好。</li>
</ol>
<p>具体使用如BeanDefinitionRegistry，BeanFactory ，BeanFactory 定义获取bean的接口，BeanDefinitionRegistry定义注册beanDefinition的接口，是获取和注册功能拆分，在获取的时候依赖于BeanFactory ，在注册的时候依赖于BeanDefinitionRegistry，避免获取的时候进行注册等越界操作</p>
<p><strong>综合各种面向对象的原则，都是从不同较多描述了 对 ”抽象“ 的使用，开闭原则是从开发目标的角度描述了抽象，替换原则从如何实现抽象的角度描述了抽象，依赖倒置原则从编程方式的角度描述了抽象，接口隔离原则从责任及安全的角度描述了抽象，总的来说都是对现实世界抽象的使用，以及抽象能带来的益处的总结。</strong></p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Design</tag>
      </tags>
  </entry>
  <entry>
    <title>如何做规划</title>
    <url>/2021/09/15/Knowledge/guihua/</url>
    <content><![CDATA[<p><u><em><strong>做事先规划是做事的一种方式</strong></em></u></p>
<h2 id="应该学会规划"><a href="#应该学会规划" class="headerlink" title="应该学会规划"></a>应该学会规划</h2><p style="text-indent:2em">
由于早起工作的态度问题，在接到任务后就总是凑齐技术要素，知道别人要什么就着手开始做，并能很快的完成并交付任务，这是一种被动接受的心态，有时也做规划，但是规划的层次比较低，面向的都是结果。时间如梭，多年后发现，高层次规划是提升和锻炼自己很有效的方法，思考问题的方式从如何完成目标任务，到了解事件背景，解决什么问题，再到深层次目标，输入的问题更多，你做的规划方案也就不一样，技术选型也不一样，架构设计也不一样。
</p>
<p style="text-indent:2em">
规划是输入问题经过思考后并给出解决问题实行方案的过程的结果，这个过程是通用的，是需要经过低层次规划到高层次规划锻炼才能熟练的，并且可以适用到大事小事，个人或者团队的事。
</p>


<h2 id="规划具有重要的意义"><a href="#规划具有重要的意义" class="headerlink" title="规划具有重要的意义"></a>规划具有重要的意义</h2><h4 id="聚焦目标"><a href="#聚焦目标" class="headerlink" title="聚焦目标"></a>聚焦目标</h4><p style="text-indent:2em">
规划的一个最为重要的作用就是让目标更清晰。团队里的人知道要做什么，有一部分人觉得迷茫，很大程度是目标不清楚，好像知道要做什么事，又不知道如何下手。这个目标可大可小，小的方面就是要完成一个项目或一个需求，大的方面就是要建立一套体系。笔者曾经就有这样的切身经历，有一段时间显得非常迷茫，不知道自己要做什么，人一迷茫就会焦虑。一旦有这样的状态，就要思考是不是目标不够清晰，目标度量是不是不够细。当目标明确之后，大家"心向一处想，劲往一处使"，这样的工作状态效率是非常高的，团队的氛围也是非常好的。
</p>

<h4 id="洞察背后逻辑"><a href="#洞察背后逻辑" class="headerlink" title="洞察背后逻辑"></a>洞察背后逻辑</h4><p style="text-indent:2em">
规划出来的内容是结果，最为重要的是思考为什么要做这些事，背后的逻辑才是核心。分析当前的现状、痛点、业界发展状况……结合业务本身，去做一些决策、取舍。不同的人思考出发点不一样，大家在一起碰撞时，会有一些想法闪现出来，也是思想的交流。我们做事不仅仅要懂得how，更要懂得why，当把why讲清楚之后，how也就是水到渠成的事，往往我们的一个困惑是不知道why。
</p>

<h4 id="知晓未来方向"><a href="#知晓未来方向" class="headerlink" title="知晓未来方向"></a>知晓未来方向</h4><p style="text-indent:2em">
规划有短期规划，也有长期规划，一般笔者是先思考长期规划，思考3年之后，我们要具备怎样的能力，再回到短期上，我们当下这个阶段要做到怎样的程度。有些事不紧急，但很重要，可以放晚一点做；有的事非常重要，又非常紧急，优先级就要提高。当我们知道了方向之后，即使当下没有去做一些事，也知道将来我们要做成什么样。以笔者的过往经历，一个团队有一两年的长远规划，团队里的成员达成共识之后，这个团队在一两年内是稳定的，因为大家知道两年之后我们会做成什么样，远方一直有一个目标在照亮着我们，大家一起努力不断逼近那个目标。
</p>
<img src="/2021/09/15/Knowledge/guihua/640.webp" class="" title="规划">


<h2 id="规划的两种思维"><a href="#规划的两种思维" class="headerlink" title="规划的两种思维"></a>规划的两种思维</h2><h4 id="归纳思维"><a href="#归纳思维" class="headerlink" title="归纳思维"></a>归纳思维</h4><p style="text-indent:2em">规划使用的第一种思维是归纳法。这是大家最常用的方法，一般是从现状出发，思考当下有哪些问题、业界是怎么做的，从这些输入中去归纳我们需要去做什么。比如当下工单咨询比较多，那么就需要对工单进行分类，如归纳成产品设计问题、程序代码问题、依赖方问题、交互设计问题，针对每类问题，再去思考我们应该要做到哪些工作。在店铺平台建设中的规划中，我们就梳理了当下的一些痛点问题，如平台只有一些基础的能力，没有给业务方提供强大的能力支撑，那么接下来就要加强店铺能力建设，丰富店铺能力。</p>
<p style="text-indent:2em">归纳思维有一个缺点就是就事论事，解决了表层的问题，比如竞品有什么，我们没有该项能力，是不是我们就要去做同样的事。归纳思维要配合深度思考一起做规划设计，真正挖掘出我们当下的痛点是什么，比如一个工单，用户只会提出他遇到了什么问题，此时就要深入分析它的root cause，有的时候只需要一个清晰的提示文案就可能解决用户的问题。</p>

<h4 id="演绎思维"><a href="#演绎思维" class="headerlink" title="演绎思维"></a>演绎思维</h4><p style="text-indent:2em">规划使用的第二种思维是演绎思维。演绎思维是一种更高级的思维模式，它需要一个人对事物有非常深刻的认识，当洞察出了底层的规律之后，再去思考我们要做什么。一般的人并不习惯用这种思维模式，它对人的要求非常高，一旦掌握之后，规划也就容易得多，很多科学家更偏向使用演绎思维。</p>
<p style="text-indent:2em">在店铺规划中，笔者就运用了演绎的思维。比如在规划之前，笔者就思考两个问题：店铺的本质是什么、店铺要提供的商业价值是什么。当把这两个问题想清楚之后，沿着价值方向去思考我们应该要给用户提供怎样的能力，这是一步一步推导出来的。店铺的本质是一个"场"，它连接了卖家和买家。针对这两种角色，卖家最为关注的是怎么使用得好店铺，包含使用得爽、店铺转化好；买家最为关注的是怎么通过最少的钱、最少的时间买到心仪的商品。沿着卖家和买家关注点，继续思考我们怎么让卖家用得爽、用得好呢？其中一个点是我们要知道卖家现在有哪些不好的体验，一定要能度量得出来，而不是定性地去分析，当定量地度量出体验指标之后，这样就可以非常有针对性地去做一些体验优化工作。站在买家的角度，可以给买家一些优惠，让他更愿意购物，这也即是我们规划店铺营销的初衷。</p>

<img src="/2021/09/15/Knowledge/guihua/641.webp" class="" title="规划">





<blockquote><footer><strong>阿里技术</strong><cite><a href="https://mp.weixin.qq.com/s/LILm--w9vj_K7qjI4EakBw">如何做规划？分享2种思维和4个方法</a></cite></footer></blockquote>
]]></content>
      <categories>
        <category>知识</category>
      </categories>
      <tags>
        <tag>Design</tag>
      </tags>
  </entry>
  <entry>
    <title>问题归类</title>
    <url>/2021/09/15/java/Interviewquestions/</url>
    <content><![CDATA[<h1 id="语言基础"><a href="#语言基础" class="headerlink" title="语言基础"></a>语言基础</h1><h2 id="并发编程"><a href="#并发编程" class="headerlink" title="并发编程"></a>并发编程</h2><p>java中守护线程和本地线程的区别</p>
<p>java中ReadWriteLock是什么</p>
<p>volatile变量和atomic变量有什么区别</p>
<p>线程优先级的理解</p>
<h2 id="jvm"><a href="#jvm" class="headerlink" title="jvm"></a>jvm</h2><p>可达性分析算法中根节点有哪些</p>
<p>CMS和G1区别有哪些</p>
<p>怎样GC调优</p>
<p>怎样排查CPU彪高内存彪高</p>
<p>逃逸分析</p>
<h1 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h1><h1 id="算法"><a href="#算法" class="headerlink" title="算法"></a>算法</h1><h1 id="框架类"><a href="#框架类" class="headerlink" title="框架类"></a>框架类</h1><h2 id="spring"><a href="#spring" class="headerlink" title="spring"></a>spring</h2><h3 id="spring-1"><a href="#spring-1" class="headerlink" title="spring"></a>spring</h3><p>spring循环依赖怎么解决</p>
<p>spring aop原理及相关的概念</p>
<p>springbean生命周期，设计思路及扩展</p>
<p>有哪些不同类型的IOC方式</p>
<h3 id="springboot"><a href="#springboot" class="headerlink" title="springboot"></a>springboot</h3><h3 id="springcloud"><a href="#springcloud" class="headerlink" title="springcloud"></a>springcloud</h3><h3 id="springmvc"><a href="#springmvc" class="headerlink" title="springmvc"></a>springmvc</h3><h2 id="Mybatis"><a href="#Mybatis" class="headerlink" title="Mybatis"></a>Mybatis</h2><p>mybatis框架的缺点是什么</p>
<p>mybatis与hibernate有哪些不同</p>
<p>mybatis是如何进行分页的分页插件的原理是什么</p>
<h1 id="微服务"><a href="#微服务" class="headerlink" title="微服务"></a>微服务</h1><h2 id="分布式一致性问题"><a href="#分布式一致性问题" class="headerlink" title="分布式一致性问题"></a>分布式一致性问题</h2><h2 id="缓存及缓存一致性问题"><a href="#缓存及缓存一致性问题" class="headerlink" title="缓存及缓存一致性问题"></a>缓存及缓存一致性问题</h2><h2 id="分布式事务问题"><a href="#分布式事务问题" class="headerlink" title="分布式事务问题"></a>分布式事务问题</h2><h2 id="zookeeper"><a href="#zookeeper" class="headerlink" title="zookeeper"></a>zookeeper</h2><p>应用场景有哪些</p>
<h1 id="数据库"><a href="#数据库" class="headerlink" title="数据库"></a>数据库</h1><h2 id="reids"><a href="#reids" class="headerlink" title="reids"></a>reids</h2><p>redis数据类型</p>
<p>redisqps能上多少，怎么知道的</p>
<p>redis单线程为什么快</p>
<p>本地缓存需要高时效性怎么办</p>
<p>redis同步机制</p>
<p>redis如何实现分布式锁，他是怎么回事</p>
<h2 id="数据库锁"><a href="#数据库锁" class="headerlink" title="数据库锁"></a>数据库锁</h2><h1 id="中间件"><a href="#中间件" class="headerlink" title="中间件"></a>中间件</h1><h2 id="tomcat"><a href="#tomcat" class="headerlink" title="tomcat"></a>tomcat</h2><p>tomcat的缺省端口是多少，怎么改</p>
<p>tomcat有哪几种connector运行模式（优化）？</p>
<p>tomcat有哪几种部署方式</p>
<p>tomca容器如何创建servlet类实例的，用了什么原理</p>
<p>tomcat如何优化</p>
<p>tomcat内存调优</p>
<p>垃圾回收策略调优</p>
<p>添加jms远程监控</p>
<p>专业点的分析工具有哪些</p>
<p>关于tomcat的session数目</p>
<p>监视tomcat的内存使用情况</p>
<p>tomcat的一个请求的完整过程</p>
<p>tomcat的工作模式</p>
<h2 id="nginx"><a href="#nginx" class="headerlink" title="nginx"></a>nginx</h2><h1 id="设计原则"><a href="#设计原则" class="headerlink" title="设计原则"></a>设计原则</h1><h1 id="设计模式类"><a href="#设计模式类" class="headerlink" title="设计模式类"></a>设计模式类</h1><h1 id="场景类"><a href="#场景类" class="headerlink" title="场景类"></a>场景类</h1><h1 id="工具类"><a href="#工具类" class="headerlink" title="工具类"></a>工具类</h1><h2 id="mavne"><a href="#mavne" class="headerlink" title="mavne"></a>mavne</h2><p>maven有哪些优点和缺点</p>
<p>maven坐标</p>
<p>maven常见的依赖范围有哪些</p>
<p>maven的生命周期</p>
<p>依赖解析机制</p>
<p>插件解析机制</p>
<p>多模块如何聚合</p>
<p>对于一个多模块项目如何管理依赖版本</p>
<p>如何保证传递依赖不起冲突</p>
<p>常见的maven私服仓库类型</p>
<p>如何查询一个插件有哪些目标</p>
<p>maven和ant有什么区别</p>
<p>maven的工程类型有哪些</p>
<p>maven的常用命令有哪些</p>
<h1 id="管理类"><a href="#管理类" class="headerlink" title="管理类"></a>管理类</h1>]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>questions</tag>
      </tags>
  </entry>
  <entry>
    <title>全局异常处理器HandlerExceptionResolver</title>
    <url>/2021/09/19/spring/HandlerExceptionResolver/</url>
    <content><![CDATA[<p>处理器异常解析器，是 DispatcherServlet 在请求转发<strong>处理过程中出现异常</strong>（请求映射处理器，处理链，处理适配器，处理器执行）的时候对异常进行处理的最后一步，，<strong>如果抛出的异常没有被处理器异常解析器HandlerExceptionResolver</strong>处理并且解析成<strong>ModelAndView</strong>，那么异常 将被抛出并交给上层容器来处理，如：我们在容器中配置的错误码映射视图</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">web-app</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 根据状态码 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">error-page</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">error-code</span>&gt;</span>500<span class="tag">&lt;/<span class="name">error-code</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">location</span>&gt;</span>/500.jsp<span class="tag">&lt;/<span class="name">location</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">error-page</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 根据异常类型 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">error-page</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">exception-type</span>&gt;</span>java.lang.RuntimeException<span class="tag">&lt;/<span class="name">exception-type</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">location</span>&gt;</span>/500.jsp<span class="tag">&lt;/<span class="name">location</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">error-page</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">welcome-file-list</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">welcome-file</span>&gt;</span>/<span class="tag">&lt;/<span class="name">welcome-file</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">welcome-file</span>&gt;</span>/index<span class="tag">&lt;/<span class="name">welcome-file</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">welcome-file-list</span>&gt;</span></span><br><span class="line">    </span><br><span class="line"><span class="tag">&lt;/<span class="name">web-app</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h1 id="SpringMvc异常处理方式"><a href="#SpringMvc异常处理方式" class="headerlink" title="SpringMvc异常处理方式"></a>SpringMvc异常处理方式</h1><ol>
<li><p>handler内部捕获处理，不往上层程序抛异常，单独处理</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;testExp&quot;)</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testExp</span><span class="params">()</span><span class="keyword">throws</span> Exception</span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> Exception(<span class="string">&quot;a&quot;</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>@ExceptionHandler注解方式。注解方式也有两种用法： </p>
<ul>
<li>使用在<code>Controller</code>内部 ，可以在Controller<strong>局部</strong>异常统一处理</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HalloWordController</span> </span>&#123;</span><br><span class="line">  <span class="comment">//可以对HalloWordController 内部所有映射的处理器进行异常捕获，他只处理HalloWordController内部抛出的异常</span></span><br><span class="line">    <span class="meta">@ExceptionHandler(Exception.class)</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">handlerException</span><span class="params">(Exception e)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>配置<code>@ControllerAdvice</code>一起使用实现<strong>全局</strong>处理,或者<strong>部分区域</strong>异常处理，<strong>取决于扫描包</strong></li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ControllerAdvice(basePackages=&#123;&quot;com.cx.user.controller&quot;&#125;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HalloWordController</span> </span>&#123;</span><br><span class="line">    <span class="comment">//可以对com.cx.user.controller包 内部所有映射的处理器进行异常捕获，他只处理HalloWordController内部抛出的异常</span></span><br><span class="line">    <span class="meta">@ExceptionHandler(Exception.class)</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">handlerException</span><span class="params">(Exception e)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>实现视图与异常进行映射处理SimpleMappingExceptionResolver</p>
</li>
<li><p>实现<code>HandlerExceptionResolver</code>方式实现<strong>全局</strong>处理</p>
</li>
</ol>
<h1 id="全局异常处理器HandlerExceptionResolver"><a href="#全局异常处理器HandlerExceptionResolver" class="headerlink" title="全局异常处理器HandlerExceptionResolver"></a>全局异常处理器HandlerExceptionResolver</h1><ol>
<li><p>doDispatch请求处理程序抛出异常，包含了整个请求处理边界</p>
</li>
<li><p>非ModelAndViewDefiningException异常的所有可捕获异常都将交给全局异常处理器里处理</p>
</li>
<li><p>异常处理器<strong>集合</strong>返回view，并将view渲染给用户,如果所有的异常处理器都未给出要渲染的view，那MVC框架将不再处理这个异常，那这个异常将抛出到上层容器来处理。</p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doDispatch</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      ModelAndView mv = <span class="keyword">null</span>;</span><br><span class="line">      Exception dispatchException = <span class="keyword">null</span>;</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">          <span class="comment">//请求处理器映射</span></span><br><span class="line">          <span class="comment">//处理链构造</span></span><br><span class="line">          <span class="comment">//处理链前置执行</span></span><br><span class="line">          <span class="comment">//执行handler</span></span><br><span class="line">          mv = ha.handle(processedRequest, response, mappedHandler.getHandler());</span><br><span class="line">          <span class="comment">//处理链后置执行</span></span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">catch</span> (Exception ex) &#123;</span><br><span class="line">         dispatchException = ex;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">catch</span> (Throwable err) &#123;</span><br><span class="line">         dispatchException = <span class="keyword">new</span> NestedServletException(<span class="string">&quot;Handler dispatch failed&quot;</span>, err);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="comment">//这里对整个的处理记过进行处理,如果MVC内部处理流程出现异常，那么将在processDispatchResult 来处理</span></span><br><span class="line">      processDispatchResult(processedRequest, response, mappedHandler, mv, dispatchException);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (Exception ex) &#123;</span><br><span class="line">      triggerAfterCompletion(processedRequest, response, mappedHandler, ex);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (Throwable err) &#123;</span><br><span class="line">      triggerAfterCompletion(processedRequest, response, mappedHandler,</span><br><span class="line">            <span class="keyword">new</span> NestedServletException(<span class="string">&quot;Handler processing failed&quot;</span>, err));</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">finally</span> &#123;</span><br><span class="line">     <span class="comment">//...</span></span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//处理转发结果，异常解析，视图渲染</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">processDispatchResult</span><span class="params">(HttpServletRequest request, HttpServletResponse response,</span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="meta">@Nullable</span> HandlerExecutionChain mappedHandler, <span class="meta">@Nullable</span> ModelAndView mv,</span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="meta">@Nullable</span> Exception exception)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">boolean</span> errorView = <span class="keyword">false</span>;</span><br><span class="line">    <span class="comment">//如果抛出异常将在这里处理</span></span><br><span class="line">   <span class="keyword">if</span> (exception != <span class="keyword">null</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (exception <span class="keyword">instanceof</span> ModelAndViewDefiningException) &#123;</span><br><span class="line">         mv = ((ModelAndViewDefiningException) exception).getModelAndView();</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">         Object handler = (mappedHandler != <span class="keyword">null</span> ? mappedHandler.getHandler() : <span class="keyword">null</span>);</span><br><span class="line">          <span class="comment">//异常处理并解析成对应的view，如果handler没有返回mv，执行的过程中抛出了异常，那么这里的异常处理器必须返回view，</span></span><br><span class="line">          <span class="comment">//如果不返回，异常将抛到上层容器，此处返回错误异常界面，后续程序将对异常界面进行渲染</span></span><br><span class="line">         mv = processHandlerException(request, response, handler, exception);</span><br><span class="line">         errorView = (mv != <span class="keyword">null</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (mv != <span class="keyword">null</span> &amp;&amp; !mv.wasCleared()) &#123;</span><br><span class="line">      render(mv, request, response);</span><br><span class="line">      <span class="keyword">if</span> (errorView) &#123;</span><br><span class="line">         WebUtils.clearErrorRequestAttributes(request);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> ModelAndView <span class="title">processHandlerException</span><span class="params">(HttpServletRequest request, HttpServletResponse response,</span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="meta">@Nullable</span> Object handler, Exception ex)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">   ModelAndView exMv = <span class="keyword">null</span>;</span><br><span class="line">   <span class="keyword">if</span> (<span class="keyword">this</span>.handlerExceptionResolvers != <span class="keyword">null</span>) &#123;</span><br><span class="line">       <span class="comment">//遍历处理异常解析器，并进行处理，直到有一个返回了view</span></span><br><span class="line">      <span class="keyword">for</span> (HandlerExceptionResolver resolver : <span class="keyword">this</span>.handlerExceptionResolvers) &#123;</span><br><span class="line">         exMv = resolver.resolveException(request, response, handler, ex);</span><br><span class="line">         <span class="keyword">if</span> (exMv != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">if</span> (exMv != <span class="keyword">null</span>) &#123;</span><br><span class="line">      <span class="comment">//返回view</span></span><br><span class="line">      <span class="keyword">return</span> exMv;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="comment">//抛出异常,</span></span><br><span class="line">   <span class="keyword">throw</span> ex;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="常用实现以及如何探测"><a href="#常用实现以及如何探测" class="headerlink" title="常用实现以及如何探测"></a>常用实现以及如何探测</h1><img src="/2021/09/19/spring/HandlerExceptionResolver/3F1EF206-8765-4d0d-8B39-B09305F9A347.png" class="" title="ExceptionResolver结构图">

<ul>
<li><p>SimpleMappingExceptionResolver</p>
<p>​    根据异常类和视图之间的映射配置来判断是否处理异常，匹配成功就返回对应的视图和对应的响应吗</p>
</li>
<li><p>ExceptionHandlerExceptionResolver</p>
<p>​    对处理器是HandlerMethod类型处理器异常进行处理，只处理一下两种情况</p>
<p>​          1. 处理器内部有@ExceptionHandler注解异常处理器，支持处理器内部区域异常处理</p>
<p>​          2. @ControllerAdvice支持全局或者部分区域（包扫描）内@ExceptionHandler注解异常处理器</p>
</li>
<li><p>DefaultHandlerExceptionResolver</p>
<p>​    处理mvc框架内部异常的处理</p>
</li>
<li><p>ResponseStatusExceptionResolver</p>
<p>​    根据国际化资源对ResponseStatusException异常进行处理</p>
</li>
<li><p>DefaultErrorAttributes</p>
<p>​     异常存储处理器不返回任何错误视图</p>
</li>
</ul>
<h2 id="SpringBoot自动装配MVC中ExceptionResolver的装配"><a href="#SpringBoot自动装配MVC中ExceptionResolver的装配" class="headerlink" title="SpringBoot自动装配MVC中ExceptionResolver的装配"></a>SpringBoot自动装配MVC中ExceptionResolver的装配</h2><figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="comment">#异常处理自动装配</span></span><br><span class="line"><span class="attr">org.springframework.boot.autoconfigure.web.servlet.error.ErrorMvcAutoConfiguration</span></span><br><span class="line"><span class="attr">org.springframework.boot.autoconfigure.web.servlet.WebMvcAutoConfiguration</span></span><br></pre></td></tr></table></figure>

<h3 id="装配DefaultErrorAttributes和BasicErrorController"><a href="#装配DefaultErrorAttributes和BasicErrorController" class="headerlink" title="装配DefaultErrorAttributes和BasicErrorController"></a>装配DefaultErrorAttributes和BasicErrorController</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//异常信息及解析器记录异常</span></span><br><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="meta">@ConditionalOnMissingBean(value = ErrorAttributes.class, search = SearchStrategy.CURRENT)</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> DefaultErrorAttributes <span class="title">errorAttributes</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> DefaultErrorAttributes();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="meta">@ConditionalOnMissingBean(value = ErrorController.class, search = SearchStrategy.CURRENT)</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> BasicErrorController <span class="title">basicErrorController</span><span class="params">(ErrorAttributes errorAttributes, ObjectProvider&lt;ErrorViewResolver&gt; errorViewResolvers)</span> </span>&#123;</span><br><span class="line">   <span class="keyword">return</span> <span class="keyword">new</span> BasicErrorController(errorAttributes, <span class="keyword">this</span>.serverProperties.getError(),errorViewResolvers.orderedStream().collect(Collectors.toList()));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="装配ExceptionHandlerExceptionResolver和ResponseStatusExceptionResolver"><a href="#装配ExceptionHandlerExceptionResolver和ResponseStatusExceptionResolver" class="headerlink" title="装配ExceptionHandlerExceptionResolver和ResponseStatusExceptionResolver"></a>装配ExceptionHandlerExceptionResolver和ResponseStatusExceptionResolver</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//WebMvcAutoConfiguration -&gt;EnableWebMvcConfiguration -&gt;DelegatingWebMvcConfiguration -&gt;WebMvcConfigurationSupport</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">addDefaultHandlerExceptionResolvers</span><span class="params">(List&lt;HandlerExceptionResolver&gt; exceptionResolvers,</span></span></span><br><span class="line"><span class="params"><span class="function">      ContentNegotiationManager mvcContentNegotiationManager)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   ExceptionHandlerExceptionResolver exceptionHandlerResolver = createExceptionHandlerExceptionResolver();</span><br><span class="line">   exceptionHandlerResolver.setContentNegotiationManager(mvcContentNegotiationManager);</span><br><span class="line">   exceptionHandlerResolver.setMessageConverters(getMessageConverters());</span><br><span class="line">   exceptionHandlerResolver.setCustomArgumentResolvers(getArgumentResolvers());</span><br><span class="line">   exceptionHandlerResolver.setCustomReturnValueHandlers(getReturnValueHandlers());</span><br><span class="line">   <span class="keyword">if</span> (jackson2Present) &#123;</span><br><span class="line">      exceptionHandlerResolver.setResponseBodyAdvice(</span><br><span class="line">            Collections.singletonList(<span class="keyword">new</span> JsonViewResponseBodyAdvice()));</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">if</span> (<span class="keyword">this</span>.applicationContext != <span class="keyword">null</span>) &#123;</span><br><span class="line">      exceptionHandlerResolver.setApplicationContext(<span class="keyword">this</span>.applicationContext);</span><br><span class="line">   &#125;</span><br><span class="line">   exceptionHandlerResolver.afterPropertiesSet();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//注册ExceptionHandlerExceptionResolver，</span></span><br><span class="line">    <span class="comment">//处理@ExceptionHandler注解的内部异常处理器</span></span><br><span class="line">    <span class="comment">//@ControllerAdvice内@ExceptionHandler注解的异常处理器，支持全局或者部分区域内部（包扫描）异常处理，</span></span><br><span class="line">   exceptionResolvers.add(exceptionHandlerResolver);</span><br><span class="line"></span><br><span class="line">   ResponseStatusExceptionResolver responseStatusResolver = <span class="keyword">new</span> ResponseStatusExceptionResolver();</span><br><span class="line">   responseStatusResolver.setMessageSource(<span class="keyword">this</span>.applicationContext);</span><br><span class="line">   exceptionResolvers.add(responseStatusResolver);</span><br><span class="line">   <span class="comment">//注册ResponseStatusExceptionResolver</span></span><br><span class="line">   exceptionResolvers.add(<span class="keyword">new</span> DefaultHandlerExceptionResolver());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="自定义全区异常处理器"><a href="#自定义全区异常处理器" class="headerlink" title="自定义全区异常处理器"></a>自定义全区异常处理器</h1><h3 id="SimpleMappingExceptionResolver映射异常处理器"><a href="#SimpleMappingExceptionResolver映射异常处理器" class="headerlink" title="SimpleMappingExceptionResolver映射异常处理器"></a>SimpleMappingExceptionResolver映射异常处理器</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Order(Integer.MAX_VALUE)</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MvcConfig</span> <span class="keyword">implements</span> <span class="title">WebMvcConfigurer</span> , <span class="title">InitializingBean</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Properties exceptionMappings;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Properties statusCodes;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">//加载映射配置</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">afterPropertiesSet</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        exceptionMappings = <span class="keyword">new</span> Properties();</span><br><span class="line">        statusCodes = <span class="keyword">new</span> Properties();</span><br><span class="line">        exceptionMappings.load(<span class="keyword">new</span> ClassPathResource(<span class="string">&quot;exceptionMappings.properties&quot;</span>).getInputStream());</span><br><span class="line">        statusCodes.load(<span class="keyword">new</span> ClassPathResource(<span class="string">&quot;statusCodes.properties&quot;</span>).getInputStream());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">configureHandlerExceptionResolvers</span><span class="params">(List&lt;HandlerExceptionResolver&gt; resolvers)</span> </span>&#123;</span><br><span class="line">        SimpleMappingExceptionResolver simpleMappingExceptionResolver = <span class="keyword">new</span> SimpleMappingExceptionResolver();</span><br><span class="line">        simpleMappingExceptionResolver.setExceptionMappings(exceptionMappings);</span><br><span class="line">        simpleMappingExceptionResolver.setStatusCodes(statusCodes);</span><br><span class="line">        simpleMappingExceptionResolver.setDefaultErrorView(<span class="string">&quot;error&quot;</span>);</span><br><span class="line">        simpleMappingExceptionResolver.setDefaultStatusCode(HttpServletResponse.SC_INTERNAL_SERVER_ERROR);</span><br><span class="line">        resolvers.add(simpleMappingExceptionResolver);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>异常视图映射配置：exceptionMappings.properties</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">异常对应视图</span></span><br><span class="line"><span class="meta">java.lang.Exception</span>=<span class="string">500</span></span><br></pre></td></tr></table></figure>

<p>视图响应码配置：statusCodes.properties</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="comment">#视图对应响应码</span></span><br><span class="line"><span class="attr">500</span>=<span class="string">500</span></span><br></pre></td></tr></table></figure>

<img src="/2021/09/19/spring/HandlerExceptionResolver/DE0925E8-7B94-40e2-8E5B-78EF1C311FBF.png" class="" title="500视图">

<h3 id="自定义实现HandlerExceptionResolver异常处理"><a href="#自定义实现HandlerExceptionResolver异常处理" class="headerlink" title="自定义实现HandlerExceptionResolver异常处理"></a>自定义实现HandlerExceptionResolver异常处理</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GobalExceptionResolver</span> <span class="keyword">implements</span> <span class="title">HandlerExceptionResolver</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line"><span class="function"><span class="keyword">public</span> ModelAndView <span class="title">resolveException</span><span class="params">(HttpServletRequest request, HttpServletResponse response,</span></span></span><br><span class="line"><span class="params"><span class="function">                                     Object handler,Exception ex)</span> </span>&#123;</span><br><span class="line">        String msg = <span class="string">&quot;服务暂停&quot;</span>;</span><br><span class="line">        <span class="comment">//如果是HandlerMethod，那么检查是否是返回ResponseBody，确定是返回界面还是json数据</span></span><br><span class="line">        <span class="keyword">if</span>(ex!=<span class="keyword">null</span> &amp;&amp; handler <span class="keyword">instanceof</span> HandlerMethod)&#123;</span><br><span class="line">            HandlerMethod hdlm =  (HandlerMethod)handler;</span><br><span class="line">            ResponseBody have_rb = AnnotationUtils.findAnnotation(hdlm.getBeanType(),ResponseBody.class);</span><br><span class="line">            <span class="comment">//返回json视图</span></span><br><span class="line">            <span class="keyword">if</span>(hdlm.hasMethodAnnotation(ResponseBody.class) || have_rb!=<span class="keyword">null</span>)&#123;</span><br><span class="line">                ModelAndView mv =   <span class="keyword">new</span> ModelAndView();</span><br><span class="line">                MappingJackson2JsonView mappingJackson2JsonView = <span class="keyword">new</span> MappingJackson2JsonView();</span><br><span class="line">                mv.setView(mappingJackson2JsonView);</span><br><span class="line">                mv.addObject(<span class="string">&quot;msg&quot;</span>,msg);</span><br><span class="line">                <span class="keyword">return</span> mv;</span><br><span class="line">           <span class="comment">//返回view视图 html     </span></span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">new</span> ModelAndView(<span class="string">&quot;error&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//返回模式视图</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> ModelAndView(<span class="string">&quot;error&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>springmvc</tag>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>镜像构建</title>
    <url>/2021/10/28/docker/imagebuild/</url>
    <content><![CDATA[<p>[TOC]</p>
<img class="576507.png &quot;docker架构&quot; &quot;docker&quot;">

<h2 id="Docker镜像的创建方法"><a href="#Docker镜像的创建方法" class="headerlink" title="Docker镜像的创建方法"></a>Docker镜像的创建方法</h2><p>基于Dockerfile进行创建，docker build .</p>
<p>基于容器进行创建</p>
<h2 id="基于Dockerfile创建"><a href="#基于Dockerfile创建" class="headerlink" title="基于Dockerfile创建"></a>基于Dockerfile创建</h2><h3 id="Dockerfile构建需要的信息："><a href="#Dockerfile构建需要的信息：" class="headerlink" title="Dockerfile构建需要的信息："></a>Dockerfile构建需要的信息：</h3><p><strong>Dockerfile、Context 和 .dockerignore</strong>，build指令的执行时在Dorker daemon程序中，第一步就是地递归的将环境Context发送到daemon段，通常情况下会采用一个空的目录作为Context,并将Dockerfile文件至于Context目录中</p>
<p>通常情况下Context下要有一个名称为Dockerfile的文件，可以通过-f 指定任意位置的Dockerfile 文件</p>
<p>我们可以通过.dockerignore文件忽略上下文环境下的文件和目录</p>
<p>我们可以通过-t 来指定新的镜像的repository和标签信息,可以指定多个 -t 来对多个repository仓库的镜像image进行打标签</p>
<p>如：docker build -t shykes/myapp:1.0.2 -t shykes/myapp:latest .</p>
<h3 id="一个Dockerfile例子"><a href="#一个Dockerfile例子" class="headerlink" title="一个Dockerfile例子"></a>一个Dockerfile例子</h3><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">FROM</span> centos:<span class="number">7</span></span><br><span class="line"><span class="keyword">MAINTAINER</span> chenxiao@ruiyinxin.com</span><br><span class="line"><span class="keyword">LABEL</span><span class="bash"> version=<span class="string">&quot;0.2&quot;</span></span></span><br><span class="line"><span class="keyword">LABEL</span><span class="bash"> description=<span class="string">&quot;测试创建sentinel&quot;</span></span></span><br><span class="line"><span class="keyword">VOLUME</span><span class="bash"> [<span class="string">&quot;/home/tomcat/log/&quot;</span>]</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> mkdir -p /app/jdk</span></span><br><span class="line"><span class="keyword">COPY</span><span class="bash"> jdk /app/jdk</span></span><br><span class="line"><span class="keyword">COPY</span><span class="bash"> sentinel-dashboard.jar /app/</span></span><br><span class="line"><span class="keyword">COPY</span><span class="bash"> run.sh /app</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> chmod 777 /app/run.sh</span></span><br><span class="line"><span class="keyword">ENV</span> JAVA_HOME /app/jdk</span><br><span class="line"><span class="keyword">ENV</span> PATH $PATH:$JAVA_HOME/bin</span><br><span class="line"><span class="keyword">ENV</span> CLASSPATH .:$JAVA_HOME/lib/dt.jar:$JAVA_HOME/lib/tools.jar</span><br><span class="line"><span class="keyword">EXPOSE</span> <span class="number">8088</span></span><br><span class="line"><span class="keyword">WORKDIR</span><span class="bash"> /app</span></span><br><span class="line"><span class="comment">#CMD [&quot;sh&quot;,&quot;run.sh&quot;]</span></span><br><span class="line"><span class="keyword">CMD</span><span class="bash"> [<span class="string">&quot;./run.sh&quot;</span>]</span></span><br><span class="line"><span class="comment">#ENTRYPOINT [&quot;./run.sh&quot;]</span></span><br></pre></td></tr></table></figure>

<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash">! /bin/sh</span></span><br><span class="line">java -jar -Dspring.profiles.active=test sentinel-dashboard.jar</span><br></pre></td></tr></table></figure>

<h3 id="执行docker命令来创建docker镜像"><a href="#执行docker命令来创建docker镜像" class="headerlink" title="执行docker命令来创建docker镜像"></a>执行docker命令来创建docker镜像</h3><p>命令格式为 docker build -t ImageName:TagName    Dir</p>
<ul>
<li><code>-t</code> − 给镜像加一个Tag</li>
<li><code>ImageName</code> − 给镜像起的名称</li>
<li><code>TagName</code> − 给镜像的Tag名</li>
<li><code>Dir</code> − Dockerfile所在目录</li>
</ul>
<p>docker build -t bd/sentineld  .</p>
<ul>
<li><code>.</code> 表示当前目录，即Dockerfile所在目录</li>
</ul>
<h3 id="dockerignore"><a href="#dockerignore" class="headerlink" title=".dockerignore"></a>.dockerignore</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># comment</span><br><span class="line">*/temp*</span><br><span class="line">*/*/temp*</span><br><span class="line">temp?</span><br></pre></td></tr></table></figure>



<table>
<thead>
<tr>
<th><code># comment</code></th>
<th>Ignored.</th>
</tr>
</thead>
<tbody><tr>
<td><code>*/temp*</code></td>
<td>Exclude files and directories whose names start with <code>temp</code> in any immediate subdirectory of the root. For example, the plain file <code>/somedir/temporary.txt</code> is excluded, as is the directory <code>/somedir/temp</code>.</td>
</tr>
<tr>
<td><code>*/*/temp*</code></td>
<td>Exclude files and directories starting with <code>temp</code> from any subdirectory that is two levels below the root. For example, <code>/somedir/subdir/temporary.txt</code> is excluded.</td>
</tr>
<tr>
<td><code>temp?</code></td>
<td>Exclude files and directories in the root directory whose names are a one-character extension of <code>temp</code>. For example, <code>/tempa</code> and <code>/tempb</code>are excluded.</td>
</tr>
</tbody></table>
<h2 id="指令解析"><a href="#指令解析" class="headerlink" title="指令解析"></a>指令解析</h2><h3 id="Dockerfile命令列表"><a href="#Dockerfile命令列表" class="headerlink" title="Dockerfile命令列表"></a>Dockerfile命令列表</h3><table>
<thead>
<tr>
<th><strong>指令</strong></th>
<th><strong>含义</strong></th>
</tr>
</thead>
<tbody><tr>
<td>FROM 镜像</td>
<td>指定新镜像所基于的镜像，第一条指令必须为FROM指令，每创建一个镜像就需要一条FROM指令</td>
</tr>
<tr>
<td>MAINTAINER 名字</td>
<td>说明新镜像的维护人信息</td>
</tr>
<tr>
<td>RUN命令</td>
<td>在所基于的镜像上执行命令，并提交到新的镜像中</td>
</tr>
<tr>
<td>CMD [“要运行的程序”,“参数1”,“参数2”]</td>
<td>指令启动容器时要运行的命令或者脚本，Dockerfile只能有一条CMD命令，如果指定多条则只能最后一条被执行，</td>
</tr>
<tr>
<td>EXPOSE 端口号</td>
<td>指定新镜像加载到Docker时要开启的端口（EXPOSE暴露的是容器内部端口，需要再映射到一个外部端口上）</td>
</tr>
<tr>
<td>ENV 环境变量 变量值</td>
<td>设置一个环境变量的值，会被后面的RUN使用</td>
</tr>
<tr>
<td>ADD 源文件/目录 目标文件/目录</td>
<td>将源文件复制到目标文件（与COPY的区别是将本地tar文件解压到镜像中）</td>
</tr>
<tr>
<td>COPY 源文件/目录 目标文件/目录</td>
<td>将本地主机上的文件/目录复制到目标地点，源文件/目录要与Dockerfile在相同的目录中</td>
</tr>
<tr>
<td>VOLUME [“目录”]</td>
<td>在容器中创建一个挂载点（VOLUME是宿主机中的某一个目录挂载到容器中）</td>
</tr>
<tr>
<td>USER 用户名/UID</td>
<td>指定运行容器时的用户</td>
</tr>
<tr>
<td>WORKDIR 路径</td>
<td>为后续的RUN、CMD、ENTRYPOINT指定工作目录（WORKDIR类似于cd，但是只切换目录一次，后续的RUN命令就可以写相对路径了）</td>
</tr>
<tr>
<td>ONBUILD 命令</td>
<td>指定所生成的镜像作为一个基础镜像时所要运行的命令</td>
</tr>
<tr>
<td>HEALTHCHECK</td>
<td>健康检查</td>
</tr>
<tr>
<td>ENTRYPOINT</td>
<td>指令和CMD类似，它也是用户指定容器启动时要执行的命令，但如果dockerfile中也有CMD指令，CMD中的参数会被附加到ENTRYPOINT指令的后面。 如果这时docker run命令带了参数，这个参数会覆盖掉CMD指令的参数，并也会附加到ENTRYPOINT 指令的后面。优先级：ENTRYPOINT&gt;CMD&gt;docker run</td>
</tr>
</tbody></table>
<h3 id="启动执行脚本-ENTRYPOINT-CMD"><a href="#启动执行脚本-ENTRYPOINT-CMD" class="headerlink" title="启动执行脚本 ENTRYPOINT CMD"></a>启动执行脚本 ENTRYPOINT CMD</h3><p><img src="https://img.jbzj.com/file_images/article/201803/2018031211363722.png" alt="img"></p>
<p><strong>shell 模式</strong></p>
<p>使用 shell 模式时，docker 会以 <code>/bin/sh -c &quot;task command&quot;</code> 的方式执行任务命令</p>
<p><strong>exec 模式</strong></p>
<p>使用 exec 模式时，容器中的任务进程就是容器内的 1 号进程</p>
<p>我们大概可以总结出下面几条规律：</p>
<p>   • 如果 ENTRYPOINT 使用了 shell 模式，CMD 指令会被忽略。</p>
<p>   • 如果 ENTRYPOINT 使用了 exec 模式，CMD 指定的内容被追加为 ENTRYPOINT 指定命令的参数。</p>
<p>   • 如果 ENTRYPOINT 使用了 exec 模式，CMD 也应该使用 exec 模式。</p>
<h3 id="FROM"><a href="#FROM" class="headerlink" title="FROM"></a>FROM</h3><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">FROM</span> [--platform=&lt;platform&gt;] &lt;image&gt; [AS &lt;name&gt;]</span><br><span class="line"><span class="keyword">FROM</span> [--platform=&lt;platform&gt;] &lt;image&gt;[:&lt;tag&gt;] [AS &lt;name&gt;]</span><br><span class="line"><span class="keyword">FROM</span> [--platform=&lt;platform&gt;] &lt;image&gt;[@&lt;digest&gt;] [AS &lt;name&gt;]</span><br></pre></td></tr></table></figure>

<p> Dockerfile必须是以FROM指令开始</p>
<p><code>ARG</code> 是唯一一个优先于 地位高于 FROM指令的，</p>
<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ARG</span> VERSION=latest</span><br><span class="line"><span class="keyword">FROM</span> busybox:$VERSION</span><br><span class="line"><span class="keyword">ARG</span> VERSION</span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">echo</span> <span class="variable">$VERSION</span> &gt; image_version</span></span><br></pre></td></tr></table></figure>

<p>–platform，指定镜像能被使用的平台信息，默认情况下是执行build request的平台信息被作为platform，如：linux/amd64<code>, </code>linux/arm64，windows/amd64</p>
<p><code>as name</code>通查是可选的，他可以指定自定义构建步骤的名称</p>
<h3 id="RUN"><a href="#RUN" class="headerlink" title="RUN"></a>RUN</h3><p>RUN指令执行有两种形式</p>
<p>shell形式  RUN  <command>，此指令执行在shell上下文环境中，默认是 /bin/sh -c  ’code‘</p>
<p>exec 形式 RUN  [“executable”, “param1”, “param2”]，exec 模式会将后续的参数解析成json 数组，也就是说我们的参数需要用双引号包括，而不是单引号。</p>
<p>run命令的每次执行在当前镜像的最顶层生成新的层，并提交结果，并且将在下一个步骤中使用，也就是说每层之间是独立的，shell环境也是独立的。我们可以使用  <code>\</code> 来使用多行单指令 如：</p>
<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">RUN</span><span class="bash"> /bin/bash -c <span class="string">&#x27;source $HOME/.bashrc; \</span></span></span><br><span class="line"><span class="string"><span class="bash">echo $HOME&#x27;</span></span></span><br><span class="line"><span class="comment">#两者含义相同</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> /bin/bash -c <span class="string">&#x27;source $HOME/.bashrc; echo $HOME&#x27;</span></span></span><br></pre></td></tr></table></figure>

<h3 id="CMD"><a href="#CMD" class="headerlink" title="CMD"></a>CMD</h3><ul>
<li><code>CMD [&quot;executable&quot;,&quot;param1&quot;,&quot;param2&quot;]</code> exec模式</li>
<li><code>CMD [&quot;param1&quot;,&quot;param2&quot;] 作为</code>ENTRYPOINT 的默认参数</li>
<li><code>CMD command param1 param2</code> <em>shell</em> 模式</li>
</ul>
<p>不可将run与cmd指令混淆，run指令会执行命令，并提交结果，cmd并不执行任何东西，在构建的过程当中，他只指定镜像启动预期指令</p>
<h3 id="LABEL"><a href="#LABEL" class="headerlink" title="LABEL"></a>LABEL</h3><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">LABEL</span><span class="bash"> &lt;key&gt;=&lt;value&gt; &lt;key&gt;=&lt;value&gt; &lt;key&gt;=&lt;value&gt; </span></span><br></pre></td></tr></table></figure>

<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">LABEL</span><span class="bash"> <span class="string">&quot;com.example.vendor&quot;</span>=<span class="string">&quot;ACME Incorporated&quot;</span></span></span><br><span class="line"><span class="keyword">LABEL</span><span class="bash"> com.example.label-with-value=<span class="string">&quot;foo&quot;</span></span></span><br><span class="line"><span class="keyword">LABEL</span><span class="bash"> version=<span class="string">&quot;1.0&quot;</span></span></span><br><span class="line"><span class="keyword">LABEL</span><span class="bash"> description=<span class="string">&quot;This text illustrates \</span></span></span><br><span class="line"><span class="string"><span class="bash">that label-values can span multiple lines.&quot;</span></span></span><br></pre></td></tr></table></figure>

<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">LABEL</span><span class="bash"> multi.label1=<span class="string">&quot;value1&quot;</span> multi.label2=<span class="string">&quot;value2&quot;</span> other=<span class="string">&quot;value3&quot;</span></span></span><br></pre></td></tr></table></figure>

<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">LABEL</span><span class="bash"> multi.label1=<span class="string">&quot;value1&quot;</span> \</span></span><br><span class="line"><span class="bash">      multi.label2=<span class="string">&quot;value2&quot;</span> \</span></span><br><span class="line"><span class="bash">      other=<span class="string">&quot;value3&quot;</span></span></span><br></pre></td></tr></table></figure>

<h3 id="EXPOSE"><a href="#EXPOSE" class="headerlink" title="EXPOSE"></a>EXPOSE</h3><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">EXPOSE</span> &lt;port&gt; [&lt;port&gt;/&lt;protocol&gt;...]</span><br></pre></td></tr></table></figure>

<p>默认情况下expose 使用的tcp协议，也可以显示指定</p>
<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">EXPOSE</span> <span class="number">80</span>/tcp</span><br><span class="line"><span class="keyword">EXPOSE</span> <span class="number">80</span>/udp</span><br></pre></td></tr></table></figure>

<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line">docker <span class="keyword">run</span><span class="bash"> -p 80:80/tcp -p 80:80/udp ...</span></span><br></pre></td></tr></table></figure>

<h3 id="ENV"><a href="#ENV" class="headerlink" title="ENV"></a>ENV</h3><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ENV</span> &lt;key&gt;=&lt;value&gt; ...</span><br></pre></td></tr></table></figure>

<p>env指令设置环境变量信息，那么环境变量信息，将存在于后续构建的各个阶段，</p>
<p>可以通过运行镜像，并指定环境变零的方式对镜像中设置的环境变量进行变更<code>docker run --env &lt;key&gt;=&lt;value&gt;</code>.</p>
<h3 id="ADD"><a href="#ADD" class="headerlink" title="ADD"></a>ADD</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ADD [--chown=&lt;user&gt;:&lt;group&gt;] &lt;src&gt;... &lt;dest&gt;</span><br><span class="line">ADD [--chown=&lt;user&gt;:&lt;group&gt;] [&quot;&lt;src&gt;&quot;,... &quot;&lt;dest&gt;&quot;]</span><br></pre></td></tr></table></figure>

<p>–chown指令只支持linux系统上构建的镜像，在windows环境中并不工作，如果不指定用户或者用户组，默认使用 a UID and GID of 0</p>
<p>src： 可使用通配符，并且路径都是相对于Context的相对路径</p>
<p>​         网络上的远程文件url，如果url不是以斜线结尾，那么将被报错在dest目录下，如果是以斜线结尾，那么问题名称将被推理出来，</p>
<p>​          如 ADD <a href="http://example.com/foobar/">http://example.com/foobar/</a>  将创建问文件  /foobar，URL必须有一个非平凡的路径，这样才能找到适当的文件名。<a href="http://example.com/">http://example.com</a> 不会工作</p>
<p>​          文件必须存在于Context中，不能使用../ 因为build第一步是发送context到docker daemon程 序</p>
<p>​          如果src是一个目录，那么目录的整个内容将被复制，COPY jdk /app/jdk</p>
<p>​          如果src是一个压缩文件(identity, gzip, bzip2 or xz) ，那么src将被解压到目标目录</p>
<p>dest： 目标路径如果不是以 / 开头的话是以WORKDIR为目录的相对路径</p>
<p>​        如果dest不存在，那么整个路径将被创建</p>
<p>​        如果dest不是以斜线结尾，那么dest将被认为是一个常规文件，src的内容将被写入到文件</p>
<p>​        如果src是多个资源，或者使用了通配符，那么dest必须要以斜线结尾</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ADD hom* /mydir/</span><br><span class="line">ADD hom?.txt /mydir/</span><br><span class="line">ADD test.txt relativeDir/</span><br><span class="line">ADD test.txt /absoluteDir/</span><br><span class="line"></span><br><span class="line">ADD --chown=55:mygroup files* /somedir/</span><br><span class="line">ADD --chown=bin files* /somedir/</span><br><span class="line">ADD --chown=1 files* /somedir/</span><br><span class="line">ADD --chown=10:11 files* /somedir/</span><br></pre></td></tr></table></figure>

<h3 id="COPY"><a href="#COPY" class="headerlink" title="COPY"></a>COPY</h3><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">COPY</span><span class="bash"> [--chown=&lt;user&gt;:&lt;group&gt;] &lt;src&gt;... &lt;dest&gt;</span></span><br><span class="line"><span class="keyword">COPY</span><span class="bash"> [--chown=&lt;user&gt;:&lt;group&gt;] [<span class="string">&quot;&lt;src&gt;&quot;</span>,... <span class="string">&quot;&lt;dest&gt;&quot;</span>]</span></span><br></pre></td></tr></table></figure>

<p>src : 文件必须存在于Context中</p>
<p>​        如果src是一个目录，那么他的内容和元数据信息将被拷贝到目标目录 COPY jdk /app/jdk</p>
<p>​        如果src是一个文件，并且dest 是以斜线结尾，那么文件将被拷贝到目录下，如 dest/src </p>
<p>dest: 如果目录不存在，那么整个文件目录将被创建</p>
<p>​         如果dest没有是以斜杠结尾，那么他将被看做事一个普通文件，并且src将被写入到dest</p>
<p>​          如果src是多资源文件，那么dest必须是以斜杠结尾</p>
<h3 id="ENTRYPOINT"><a href="#ENTRYPOINT" class="headerlink" title="ENTRYPOINT"></a>ENTRYPOINT</h3><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ENTRYPOINT</span><span class="bash"> [<span class="string">&quot;executable&quot;</span>, <span class="string">&quot;param1&quot;</span>, <span class="string">&quot;param2&quot;</span>]   <span class="comment">#exec模式</span></span></span><br><span class="line"><span class="keyword">ENTRYPOINT</span><span class="bash"> <span class="built_in">command</span> param1 param2                <span class="comment">#shell模式</span></span></span><br></pre></td></tr></table></figure>

<ol>
<li>Dockerfile 最少需要制定一个CMD或者ENTRYPOINT指令</li>
<li>当容器是可执行的情况下，应该定义ENTRYPOINT</li>
<li>CMD应该被用来定义ENTRYPOINT 的默认参数，</li>
<li>CMD参数可以在容器运行的时候被替换掉，ENTRYPOINT则不行</li>
</ol>
<table>
<thead>
<tr>
<th align="left"></th>
<th align="left"><strong>No ENTRYPOINT</strong></th>
<th align="left"><strong>ENTRYPOINT exec_entry p1_entry</strong></th>
<th><strong>ENTRYPOINT [“exec_entry”, “p1_entry”]</strong></th>
</tr>
</thead>
<tbody><tr>
<td align="left"><strong>No CMD</strong></td>
<td align="left"><em>error, not allowed</em></td>
<td align="left">/bin/sh -c exec_entry p1_entry</td>
<td>exec_entry p1_entry</td>
</tr>
<tr>
<td align="left"><strong>CMD [“exec_cmd”, “p1_cmd”]</strong></td>
<td align="left">exec_cmd p1_cmd</td>
<td align="left">/bin/sh -c exec_entry p1_entry</td>
<td>exec_entry p1_entry exec_cmd p1_cmd</td>
</tr>
<tr>
<td align="left"><strong>CMD [“p1_cmd”, “p2_cmd”]</strong></td>
<td align="left">p1_cmd p2_cmd</td>
<td align="left">/bin/sh -c exec_entry p1_entry</td>
<td>exec_entry p1_entry p1_cmd p2_cmd</td>
</tr>
<tr>
<td align="left"><strong>CMD exec_cmd p1_cmd</strong></td>
<td align="left">/bin/sh -c exec_cmd p1_cmd</td>
<td align="left">/bin/sh -c exec_entry p1_entry</td>
<td>exec_entry p1_entry /bin/sh -c exec_cmd p1_cmd</td>
</tr>
</tbody></table>
<h3 id="VOLUME"><a href="#VOLUME" class="headerlink" title="VOLUME"></a>VOLUME</h3><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">VOLUME</span><span class="bash"> [<span class="string">&quot;/data&quot;</span>]</span></span><br></pre></td></tr></table></figure>

<p>VOLUME指令将从本地主机或者其他的容器创建一个挂载点并使用给定的名称，他的值可以是一个json数组VOLUME [“/var/log/“]，或者是一个字符串 VOLUME /var/log。 或者是多个VOLUME /var/log /var/db。</p>
<p>如果是基于windows的容器，那么这数据卷必须是一个不存在的或者是一个空的目录，并且是C盘以外的数据驱动</p>
<p>volume在声明之后，如果在build的过程中的每一个步骤对volume内容的修改都将被忽略，</p>
<p>声明的时候如果是json数据，必须是以双引号进行包括，而不是单引号</p>
<p>VOLUME指令挂在的目录，并不能通过host_dir来指定映射到主机那个目录，他是在运行容器的时候才确定的，你必须指定一个挂载点，当你创建或者启动一个容器的时候，</p>
<h3 id="USER"><a href="#USER" class="headerlink" title="USER"></a>USER</h3><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">USER</span> &lt;<span class="keyword">user</span>&gt;[:&lt;group&gt;]</span><br><span class="line"><span class="keyword">USER</span> &lt;UID&gt;[:&lt;GID&gt;]</span><br></pre></td></tr></table></figure>

<h3 id="ARG"><a href="#ARG" class="headerlink" title="ARG"></a>ARG</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ARG &lt;name&gt;[=&lt;default value&gt;]</span><br></pre></td></tr></table></figure>

<p>ARG指令可以在构建的过程中定义一些参数，并设置一些默认值，并且在build的时候可以指定</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker build --build-arg user=what_user .</span><br></pre></td></tr></table></figure>

<p><strong>默认值；</strong>如果build的时候没有指定–build-arg user1=what_user 那么someuser将被作为默认值</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">FROM busybox</span><br><span class="line">ARG user1=someuser</span><br><span class="line">ARG buildno=1</span><br></pre></td></tr></table></figure>

<p><strong>作用域：</strong>以下示例，第二行将使用some_user来作为值来使用，第四行将使用what_user，ARG指令定义的变量是从他定义的行开始生效的</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">FROM busybox</span><br><span class="line">USER $&#123;user:-some_user&#125;</span><br><span class="line">ARG user</span><br><span class="line">USER $user</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker build --build-arg user=what_user .</span><br></pre></td></tr></table></figure>

<p><strong>使用ARG变量：</strong>ENV指令将会覆盖ARG同名参数，如下将使用v1.0.0，</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">FROM ubuntu</span><br><span class="line">ARG CONT_IMG_VER</span><br><span class="line">ENV CONT_IMG_VER=v1.0.0</span><br><span class="line">RUN echo $CONT_IMG_VER</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker build --build-arg CONT_IMG_VER=v2.0.1 .</span><br></pre></td></tr></table></figure>

<p><strong>ENV和ARG</strong>：ARG变量并不能像EVN环境变量那样保存在镜像之中</p>
<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">FROM</span> ubuntu</span><br><span class="line"><span class="keyword">ARG</span> CONT_IMG_VER</span><br><span class="line"><span class="keyword">ENV</span> CONT_IMG_VER=$&#123;CONT_IMG_VER:-v1.<span class="number">0.0</span>&#125;   <span class="comment">#可带有默认值</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">echo</span> <span class="variable">$CONT_IMG_VER</span></span></span><br></pre></td></tr></table></figure>

<p>Docker中包含一些默认的ARG参数，可以不用再Dockerfile中生命</p>
<ul>
<li><p><code>HTTP_PROXY</code></p>
</li>
<li><p><code>http_proxy</code></p>
</li>
<li><p><code>HTTPS_PROXY</code></p>
</li>
<li><p><code>https_proxy</code></p>
</li>
<li><p><code>FTP_PROXY</code></p>
</li>
<li><p><code>ftp_proxy</code></p>
</li>
<li><p><code>NO_PROXY</code></p>
</li>
<li><p><code>no_proxy</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker build --build-arg HTTPS_PROXY=https://my-proxy.example.com .</span><br></pre></td></tr></table></figure></li>
</ul>
<p>Docker默认的ARG全局范围内的平台参数，这些参数是在全局范围中定义的，因此在构建阶段或运行命令中不自动可用。要在构建阶段中公开这些参数之一，请在没有值的情况下重新定义它</p>
<ul>
<li><p><code>TARGETPLATFORM</code> - platform of the build result. Eg <code>linux/amd64</code>, <code>linux/arm/v7</code>, <code>windows/amd64</code>.</p>
</li>
<li><p><code>TARGETOS</code> - OS component of TARGETPLATFORM</p>
</li>
<li><p><code>TARGETARCH</code> - architecture component of TARGETPLATFORM</p>
</li>
<li><p><code>TARGETVARIANT</code> - variant component of TARGETPLATFORM</p>
</li>
<li><p><code>BUILDPLATFORM</code> - platform of the node performing the build.</p>
</li>
<li><p><code>BUILDOS</code> - OS component of BUILDPLATFORM</p>
</li>
<li><p><code>BUILDARCH</code> - architecture component of BUILDPLATFORM</p>
</li>
<li><p><code>BUILDVARIANT</code> - variant component of BUILDPLATFORM</p>
<p>如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">FROM alpine</span><br><span class="line">ARG TARGETPLATFORM</span><br><span class="line">RUN echo &quot;I&#x27;m building for $TARGETPLATFORM&quot;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="ONBUILD"><a href="#ONBUILD" class="headerlink" title="ONBUILD"></a>ONBUILD</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ONBUILD &lt;INSTRUCTION&gt;</span><br></pre></td></tr></table></figure>

<p>这是一个触发器指令，镜像构建时候定义，当别的镜像基于当前镜像进行构建的时候 ONBUILD指令将在FROM指令后首先触发基础镜像的ONBUILD指令，</p>
<p>他注册了一个超前指令，并在之后运行，运行环境是构建容器的Context，如果有一个指令失败，那么FROM指令将是失败的</p>
<p>他能在docker inspect指令中被观察到</p>
<p>ONBUILD 只在直接FROM Dockerfile中生效，也就是多级A FROM B FROM C ,那么c的ONBILD 不会再A中生效</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ONBUILD ADD . /app/src</span><br><span class="line">ONBUILD RUN /usr/local/bin/python-build --dir /app/src</span><br></pre></td></tr></table></figure>

<p>ONBUILD不支持连续使用，ONBUILD ONBUILD</p>
<p>ONBUILD不会触发FROM 和 MAINTAINER指令</p>
<h3 id="STOPSIGNAL"><a href="#STOPSIGNAL" class="headerlink" title="STOPSIGNAL"></a>STOPSIGNAL</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">STOPSIGNAL signal</span><br></pre></td></tr></table></figure>

<p>默认的STOPSIGNAL 可以使用–stop-signal 标记，在docker run 和docker create 的时候修改掉</p>
<h3 id="HEALTHCHECK"><a href="#HEALTHCHECK" class="headerlink" title="HEALTHCHECK"></a>HEALTHCHECK</h3><ul>
<li><p><code>HEALTHCHECK [OPTIONS] CMD command</code> (check container health by running a command inside the container)</p>
</li>
<li><p><code>HEALTHCHECK NONE</code> (disable any healthcheck inherited from the base image)</p>
<p>HEALTHCHECK 指令告诉Docker如何测试容器是否在工作。</p>
<p>当指定了HEALTHCHECK 那么容器除了正常的状态，会添加一些健康状态，初始化是starting，当健康检查通过，状态为healthy，当经理了一系列的故障之后，将变更为unhealthy</p>
<ul>
<li><code>--interval=DURATION</code> (default: <code>30s</code>)</li>
<li><code>--timeout=DURATION</code> (default: <code>30s</code>)</li>
<li><code>--start-period=DURATION</code> (default: <code>0s</code>)</li>
<li><code>--retries=N</code> (default: <code>3</code>)</li>
</ul>
</li>
<li><p>0: success - the container is healthy and ready for use</p>
</li>
<li><p>1: unhealthy - the container is not working correctly</p>
</li>
<li><p>2: reserved - do not use this exit code 预留的状态</p>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">HEALTHCHECK --interval=5m --timeout=3s CMD curl -f http://localhost/ || exit 1</span><br></pre></td></tr></table></figure>

<h3 id="SHELL"><a href="#SHELL" class="headerlink" title="SHELL"></a>SHELL</h3><p>shell指令运行将默认的shell进行调整，默认的linux shell 是[“/bin/sh”, “-c”] Windows 是<code>[&quot;cmd&quot;, &quot;/S&quot;, &quot;/C&quot;]</code></p>
<p>shell ，在Dockerfile中必须是json模式的</p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>linux用户及权限</title>
    <url>/2021/09/22/linux/userp/</url>
    <content><![CDATA[<p>linux 用户管理</p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis高可用</title>
    <url>/2021/09/22/java/redisha/</url>
    <content><![CDATA[<h2 id="Sentinel相关配置"><a href="#Sentinel相关配置" class="headerlink" title="Sentinel相关配置"></a>Sentinel相关配置</h2><p>bind 12.3.10.222</p>
<p>port 26377</p>
<p>sentinel monitor mymaster 12.3.10.222 6377 2</p>
<p>sentinel auth-pass mymaster cx</p>
<p>sentinel down-after-milliseconds mymaster 30000</p>
<p>sentinel parallel-syncs mymaster 3</p>
<p>sentinel failover-timeout mymaster 180000</p>
<p>loglevel debug    logfile “/usr/redis/log/sentinel-26377.log”     daemonize yes</p>
<p>启动方式</p>
<ul>
<li>```<br>redis-sentinel /path/to/sentinel.conf<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">- ```</span><br><span class="line">  redis-server /path/to/sentinel.conf --sentinel</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="Sentinel及服务状态感知能力"><a href="#Sentinel及服务状态感知能力" class="headerlink" title="Sentinel及服务状态感知能力"></a>Sentinel及服务状态感知能力</h2><p>环境内信息共享，sentinel与主服务从服务之间建立命令链接及频道订阅发布链接，用以感知从服务器加入，新的sentinel加入</p>
<p>​    1. sentinel每10秒向主服务发送INFO命令收集信息，收集从服务信息，<strong>发现新的服务加入</strong>，并建立从服务数据结构，建立从服务命令及频道链接，</p>
<p>​    2. 建立好从服务链接后每10秒发送INFO命令收集从服务状态信息，并更新数据结构中从服务的状态信息</p>
<p>​    3. 向主服务器从服务器发布 __sentinel__:hello  频道  共享sentinel自身状态及主服务信息，所有sentinel发布信息到此频道</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PUBLISH  __sentinel__:hello &quot;s_ip s_port s_runid s_epoch m_name m_ip m_port m_epoch&quot;</span><br><span class="line">s_ip s_port s_runid s_epoch  发送消息的sentinel信息</span><br><span class="line">m_name m_ip m_port m_epoch   发送消息的sentinel监测的master信息</span><br></pre></td></tr></table></figure>

<p>​    4. 订阅主服务器从服务器 __sentinel__:hello  频道  感知其他sentinel信息及主服务信息，所有sentinel订阅此频道，并接受消息</p>
<h2 id="状态检测：sentinel与所有服务包括主从服务sentinel服务保持命令链接"><a href="#状态检测：sentinel与所有服务包括主从服务sentinel服务保持命令链接" class="headerlink" title="状态检测：sentinel与所有服务包括主从服务sentinel服务保持命令链接"></a>状态检测：sentinel与所有服务包括主从服务sentinel服务保持命令链接</h2><p>​    sentinel除了和主服务从服务建立命令链接和频道链接外，还与其他sentinel建立命令链接，用于状态监测 PING命令执行</p>
<h3 id="主观下线状态监测-PING"><a href="#主观下线状态监测-PING" class="headerlink" title="主观下线状态监测 PING"></a>主观下线状态监测 PING</h3><ol>
<li><p>sentinel 和主从服务及其他sentinel建立连接后会以每秒一次的频率发送PING命令，并根据回复来判断实例是否下线，PING命令有固定的几个有效的回复，有效的回复有+PONG，-LOADING,-MASTERDOWN三种回复，除了三种回复的结果都是无效回复。</p>
</li>
<li><p>sentinel配置文件中 down-after-milliseconds 配置是sentinel判断主观下线所需的时间长度，在down-after-milliseconds毫秒内服务连续向sentinel返回无效回复，那么sentinel将实例结构中flags属性中打卡 SRI-S-DOWN标识，以此来表示实例进入主管下线状态。</p>
</li>
<li><p>down-after-milliseconds 配置不仅作用在判断主从服务下线检查，也作用在sentinel下线检查时长</p>
<p>sentinel monitor mymaster host  port  quorum(客观下线所支持的投票数量)</p>
<p>sentinel down-after-milliseconds mymaster  30000</p>
</li>
</ol>
<h3 id="客观下线状态监测-SENTINEL-is-master-down-by-addr-命令"><a href="#客观下线状态监测-SENTINEL-is-master-down-by-addr-命令" class="headerlink" title="客观下线状态监测 SENTINEL  is-master-down-by-addr 命令:"></a>客观下线状态监测 SENTINEL  is-master-down-by-addr 命令:</h3><p>当sentinel判断主服务器为主观下线的时候，会向别的监视主服务的sentinel进行询问，看他们是否也认为主服务器已进入了下线状态（可已是主观也可以是客观），<strong>当接收到足够数量的下线判断</strong>（quorum）后，sentinel会将服务器判定为客观下线并进行故障转移</p>
<p>询问是否已下线请求：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">发送：SENTINEL is-master-down-by-addr ip port current_epoch  runid</span><br><span class="line">ip 被sentinel判断为主观下次的主服务ip</span><br><span class="line">port 被sentinel判断为主观下次的主服务端口</span><br><span class="line">current_epoch sentinel当前的配置纪元，用于sentinel领头选举</span><br><span class="line">runid *或者是sentinel运行id</span><br><span class="line"></span><br><span class="line">回复：down_state leader_runid leader_epoch</span><br><span class="line">down_state 1 服务已下线 0 服务未下线</span><br><span class="line">leader_runid  *或者是sentinel运行id，用于sentinel领头选举，询问阶段返回*</span><br><span class="line">leader_epoch  配置纪元，用于sentinel领头选举 询问阶段返回0</span><br><span class="line"></span><br></pre></td></tr></table></figure>



<h2 id="选举领头sentinel"><a href="#选举领头sentinel" class="headerlink" title="选举领头sentinel"></a>选举领头sentinel</h2><ul>
<li>当sentinel监测到主服务下线会进行leader选举，并由选举出的sentinel负责进行故障转移，选举的命令是通过SENTINEL is-master-down-by-addr 进行发送的，并根据返回结果进行收集，当超过半数的sentinel服务投票决定，那台sentinel将成为leader。</li>
<li>当客观quorum达到后sentinel会向别的sentinel（目标）发送SENTINEL is-master-down-by-addr ip port current_epoch  runid 命令，并携带current_epoch  ，runid </li>
<li>目标sentinel接收到is-master-down-by-addr  请求，如果当前sentinel未进行过投票，则返回发送自己的epoch  ，和请求投票的sentinel的runid表示投票，如果已经投过票，就拒绝请求</li>
<li>源sentinel接受到回复后，会比对leader_epoch 纪元是否和自己的相等，如果相等判断leader_runid是否是自己的runid如果是，表明投票给自己。</li>
<li>每个sentinel在每个纪元epoch内只能进行一次的投票，并且只能投票一次，根据请求到达的先后顺序，投票遵循先到先得，leader申请的sentinel根据的leader_runid，leader_epoch结果统计投票信息，并在过半后成为leader。</li>
<li>每个sentinel都有机会成为leader，每次lead选举无论成功或者失败epoch纪元都会增1</li>
<li>如果给定时限内没有一个sentinel被选举为leader，那么sentinel将在一段时间后再次进行选举，知道选举出leader为止。</li>
</ul>
<h2 id="实现故障转移"><a href="#实现故障转移" class="headerlink" title="实现故障转移"></a>实现故障转移</h2><p>在已下线的主服务器的从服务器里挑选一个从服务，并将其转换为主服务</p>
<ol>
<li>​     选择从服务规则（主服务中slaves列表）</li>
<li>​      删除已经下线的从服务</li>
<li>​      删除最近5秒内没有回复过leader的从服务</li>
<li>​      删除与主服务断开连接超过 down-after-milliseconds * 10 毫秒的从服务</li>
<li>​     根据从服务的优先级排序，选择优先级最高的从服务如果有多个，将按照复制偏移量最大的优先，如果优先级复制偏移量都一样，就按照runid进行排序 宣传runid最小的为主服务</li>
<li><pre><code> 向从服务发送 SLAVEOF no one 命令，调整从服务未主服务
</code></pre>
</li>
</ol>
<p>将已下线的主服务下的所有从服务，改为复制挑选出的主服务</p>
<ul>
<li>​    向未被选择的从服务发送 SLAVEOF new_master new_master_por 进行复制新的主服务命令，并调整配置文件</li>
</ul>
<p>将已下线的主服务器设置为新选择的主服务的从服务，当就的主服务重新上线是，他会成为新的主服务的从服务</p>
<ul>
<li>调整配置文件</li>
</ul>
<!--所有被调整过的服务，redis.conf 复制服务配置将被调整-->

<p>sentinel.c 关于sentinel的状态的数据结构</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Main state. */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sentinelState</span> &#123;</span></span><br><span class="line">    <span class="keyword">char</span> myid[CONFIG_RUN_ID_SIZE+<span class="number">1</span>]; <span class="comment">/* This sentinel ID. */</span></span><br><span class="line">    <span class="keyword">uint64_t</span> current_epoch;         <span class="comment">/* Current epoch. */</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* 报错所有被这个sentinel监视的服务 结构是sentinelRedisInstances */</span></span><br><span class="line">    dict *masters;      <span class="comment">/* Dictionary of master sentinelRedisInstances.</span></span><br><span class="line"><span class="comment">                           Key is the instance name, value is the</span></span><br><span class="line"><span class="comment">                           sentinelRedisInstance structure pointer. */</span></span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">    <span class="keyword">int</span> tilt;           <span class="comment">/* Are we in TILT mode? */</span></span><br><span class="line">    <span class="keyword">int</span> running_scripts;    <span class="comment">/* Number of scripts in execution right now. */</span></span><br><span class="line">    <span class="keyword">mstime_t</span> tilt_start_time;       <span class="comment">/* When TITL started. */</span></span><br><span class="line">    <span class="keyword">mstime_t</span> previous_time;         <span class="comment">/* Last time we ran the time handler. */</span></span><br><span class="line">    <span class="built_in">list</span> *scripts_queue;            <span class="comment">/* Queue of user scripts to execute. */</span></span><br><span class="line">    <span class="keyword">char</span> *announce_ip;  <span class="comment">/* IP addr that is gossiped to other sentinels if</span></span><br><span class="line"><span class="comment">                           not NULL. */</span></span><br><span class="line">    <span class="keyword">int</span> announce_port;  <span class="comment">/* Port that is gossiped to other sentinels if</span></span><br><span class="line"><span class="comment">                           non zero. */</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> simfailure_flags; <span class="comment">/* Failures simulation. */</span></span><br><span class="line">&#125; sentinel;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">sentinelRedisInstance</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> flags;      <span class="comment">/* See SRI_... defines */</span></span><br><span class="line">    <span class="keyword">char</span> *name;     <span class="comment">/* Master name from the point of view of this sentinel. */</span></span><br><span class="line">    <span class="keyword">char</span> *runid;    <span class="comment">/* Run ID of this instance, or unique ID if is a Sentinel.*/</span></span><br><span class="line">    <span class="keyword">uint64_t</span> config_epoch;  <span class="comment">/* Configuration epoch. */</span></span><br><span class="line">    sentinelAddr *addr; <span class="comment">/* Master host. */</span></span><br><span class="line">    instanceLink *link; <span class="comment">/* Link to the instance, may be shared for Sentinels. */</span></span><br><span class="line">    <span class="keyword">mstime_t</span> last_pub_time;   <span class="comment">/* Last time we sent hello via Pub/Sub. */</span></span><br><span class="line">    <span class="keyword">mstime_t</span> last_hello_time; <span class="comment">/* Only used if SRI_SENTINEL is set. Last time</span></span><br><span class="line"><span class="comment">                                 we received a hello from this Sentinel</span></span><br><span class="line"><span class="comment">                                 via Pub/Sub. */</span></span><br><span class="line">    <span class="keyword">mstime_t</span> last_master_down_reply_time; <span class="comment">/* Time of last reply to</span></span><br><span class="line"><span class="comment">                                             SENTINEL is-master-down command. */</span></span><br><span class="line">    <span class="keyword">mstime_t</span> s_down_since_time; <span class="comment">/* Subjectively down since time. */</span></span><br><span class="line">    <span class="keyword">mstime_t</span> o_down_since_time; <span class="comment">/* Objectively down since time. */</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">/* 配置 sentinel down-after-milliseconds mymaster 30000 */</span></span><br><span class="line">    <span class="keyword">mstime_t</span> down_after_period; <span class="comment">/* Consider it down after that period. */</span></span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">    <span class="keyword">mstime_t</span> info_refresh;  <span class="comment">/* Time at which we received INFO output from it. */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Role and the first time we observed it.</span></span><br><span class="line"><span class="comment">     * This is useful in order to delay replacing what the instance reports</span></span><br><span class="line"><span class="comment">     * with our own configuration. We need to always wait some time in order</span></span><br><span class="line"><span class="comment">     * to give a chance to the leader to report the new configuration before</span></span><br><span class="line"><span class="comment">     * we do silly things. */</span></span><br><span class="line">    <span class="keyword">int</span> role_reported;</span><br><span class="line">    <span class="keyword">mstime_t</span> role_reported_time;</span><br><span class="line">    <span class="keyword">mstime_t</span> slave_conf_change_time; <span class="comment">/* Last time slave master addr changed. */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* Master specific. */</span></span><br><span class="line">    dict *sentinels;    <span class="comment">/* Other sentinels monitoring the same master. */</span></span><br><span class="line">    dict *slaves;       <span class="comment">/* Slaves for this master instance. */</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> quorum;<span class="comment">/* Number of sentinels that need to agree on failure. */</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">/* 配置 sentinel parallel-syncs mymaster 3 */</span></span><br><span class="line">    <span class="keyword">int</span> parallel_syncs; <span class="comment">/* How many slaves to reconfigure at same time. */</span></span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">    <span class="keyword">char</span> *auth_pass;    <span class="comment">/* Password to use for AUTH against master &amp; slaves. */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Slave specific. */</span></span><br><span class="line">    <span class="keyword">mstime_t</span> master_link_down_time; <span class="comment">/* Slave replication link down time. */</span></span><br><span class="line">    <span class="keyword">int</span> slave_priority; <span class="comment">/* Slave priority according to its INFO output. */</span></span><br><span class="line">    <span class="keyword">mstime_t</span> slave_reconf_sent_time; <span class="comment">/* Time at which we sent SLAVE OF &lt;new&gt; */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sentinelRedisInstance</span> *<span class="title">master</span>;</span> <span class="comment">/* Master instance if it&#x27;s slave. */</span></span><br><span class="line">    <span class="keyword">char</span> *slave_master_host;    <span class="comment">/* Master host as reported by INFO */</span></span><br><span class="line">    <span class="keyword">int</span> slave_master_port;      <span class="comment">/* Master port as reported by INFO */</span></span><br><span class="line">    <span class="keyword">int</span> slave_master_link_status; <span class="comment">/* Master link status as reported by INFO */</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">long</span> slave_repl_offset; <span class="comment">/* Slave replication offset. */</span></span><br><span class="line">    <span class="comment">/* Failover */</span></span><br><span class="line">    <span class="keyword">char</span> *leader;       <span class="comment">/* If this is a master instance, this is the runid of</span></span><br><span class="line"><span class="comment">                           the Sentinel that should perform the failover. If</span></span><br><span class="line"><span class="comment">                           this is a Sentinel, this is the runid of the Sentinel</span></span><br><span class="line"><span class="comment">                           that this Sentinel voted as leader. */</span></span><br><span class="line">    <span class="keyword">uint64_t</span> leader_epoch; <span class="comment">/* Epoch of the &#x27;leader&#x27; field. */</span></span><br><span class="line">    <span class="keyword">uint64_t</span> failover_epoch; <span class="comment">/* Epoch of the currently started failover. */</span></span><br><span class="line">    <span class="keyword">int</span> failover_state; <span class="comment">/* See SENTINEL_FAILOVER_STATE_* defines. */</span></span><br><span class="line">    <span class="keyword">mstime_t</span> failover_state_change_time;</span><br><span class="line">    <span class="keyword">mstime_t</span> failover_start_time;   <span class="comment">/* Last failover attempt start time. */</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">/*刷新故障迁移状态的最大时限 sentinel failover-timeout mymaster 180000 */</span></span><br><span class="line">    <span class="keyword">mstime_t</span> failover_timeout;      <span class="comment">/* Max time to refresh failover state. */</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">mstime_t</span> failover_delay_logged; <span class="comment">/* For what failover_start_time value we</span></span><br><span class="line"><span class="comment">                                       logged the failover delay. */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sentinelRedisInstance</span> *<span class="title">promoted_slave</span>;</span> <span class="comment">/* Promoted slave instance. */</span></span><br><span class="line">    <span class="comment">/* Scripts executed to notify admin or reconfigure clients: when they</span></span><br><span class="line"><span class="comment">     * are set to NULL no script is executed. */</span></span><br><span class="line">    <span class="keyword">char</span> *notification_script;</span><br><span class="line">    <span class="keyword">char</span> *client_reconfig_script;</span><br><span class="line">    sds info; <span class="comment">/* cached INFO output */</span></span><br><span class="line">&#125; sentinelRedisInstance;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**************************************************************** sentinel可执行的命令*/</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">redisCommand</span> <span class="title">sentinelcmds</span>[] =</span> &#123;</span><br><span class="line">    &#123;<span class="string">&quot;ping&quot;</span>,pingCommand,<span class="number">1</span>,<span class="string">&quot;&quot;</span>,<span class="number">0</span>,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;sentinel&quot;</span>,sentinelCommand,<span class="number">-2</span>,<span class="string">&quot;&quot;</span>,<span class="number">0</span>,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;subscribe&quot;</span>,subscribeCommand,<span class="number">-2</span>,<span class="string">&quot;&quot;</span>,<span class="number">0</span>,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;unsubscribe&quot;</span>,unsubscribeCommand,<span class="number">-1</span>,<span class="string">&quot;&quot;</span>,<span class="number">0</span>,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;psubscribe&quot;</span>,psubscribeCommand,<span class="number">-2</span>,<span class="string">&quot;&quot;</span>,<span class="number">0</span>,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;punsubscribe&quot;</span>,punsubscribeCommand,<span class="number">-1</span>,<span class="string">&quot;&quot;</span>,<span class="number">0</span>,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;publish&quot;</span>,sentinelPublishCommand,<span class="number">3</span>,<span class="string">&quot;&quot;</span>,<span class="number">0</span>,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;info&quot;</span>,sentinelInfoCommand,<span class="number">-1</span>,<span class="string">&quot;&quot;</span>,<span class="number">0</span>,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;role&quot;</span>,sentinelRoleCommand,<span class="number">1</span>,<span class="string">&quot;l&quot;</span>,<span class="number">0</span>,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;client&quot;</span>,clientCommand,<span class="number">-2</span>,<span class="string">&quot;rs&quot;</span>,<span class="number">0</span>,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;shutdown&quot;</span>,shutdownCommand,<span class="number">-1</span>,<span class="string">&quot;&quot;</span>,<span class="number">0</span>,<span class="literal">NULL</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h1 id="Lua脚本"><a href="#Lua脚本" class="headerlink" title="Lua脚本"></a>Lua脚本</h1><p>RedisServer中嵌入了Lua环境，Redis客户端可以使用lua脚本在服务器上执行多个redis命令。</p>
<p>server在启动后初始了lua环境，lua环境初始化中载入了多个lua函数库,并创建redis全局表格（其中就包含了redis.call(),redis.pcall），并且创建给lua环境脚本执行辅助的工具，命令执行伪客户端和脚本字典（脚本字典是实现SCRIPT EXISTS和脚本复制功能根本），</p>
<p>LUA脚本命令执行步骤： (EVAL)</p>
<p>​    定义脚本函数( f_SHA1CODE)载入lua环境</p>
<p>​    保存脚本到脚本字典lua_scripts（key:SHA1CODE,value:script）</p>
<p>​    执行脚本函数(redis命令会通过，伪客户端来向server发送指令来执行，然后结果返回给伪客户端，伪客户端返回结果到lua环境)</p>
<p>LUA环境脚本管理：</p>
<p>​    SCRIPT FLUSH:清空lua环境脚本函数，并销毁并重新构建一个lua环境</p>
<p>​    SCRIPT EXISTS：判断给定的SHA1值对应的脚本是否存在于lua环境</p>
<p>​    SCRIPT LOAD：加载一个lua脚本并，创建脚本函数并加载到lua环境，并将脚本加入到脚本字典</p>
<p>​    SCRIPT KILL：lua-time-limit 超时使用</p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>redis</tag>
      </tags>
  </entry>
  <entry>
    <title>Sentinel</title>
    <url>/2021/11/05/spring/sentinel/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>SpringCloud</tag>
      </tags>
  </entry>
</search>
